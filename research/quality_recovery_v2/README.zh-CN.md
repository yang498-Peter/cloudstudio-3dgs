# quality_recovery_v2 — 按《澳洲B机_3DGS全链路审计与Agent执行任务书_2026-09-11》执行

任务书于 2026-09-11 由用户在会话中提供（连同 `3DGS_B_machine_audit_agent_pack_2026-09-11.zip`）。执行开始时（2026-09-11 06:10 AEST）桌面上已找不到这两个文件，任务书正文以会话中读取的内容为准；随附的 `audit_gs_config.py` 未取得，WP02 的日程审计改由仓库内 `cloudstudio_3dgs/training/schedule_audit.py` + `tools/audit_training_schedule.py` 实现（同时满足任务书要求的 `resolved_schedule` 纯函数）。若需用原脚本复核，请重新提供该 zip。

执行顺序遵循任务书：WP00 → WP01/WP02 → WP03 → WP04/WP05 → 依诊断选择 WP06/WP07 → WP08/WP09；WP10 的 CPU 工作并行。不一次性排满整卡。

## 与任务书对应的落地状态

| 项 | 状态 | 文件 |
| --- | --- | --- |
| 正在跑的 tile3（R1d 交付）不终止，完成后归档为 R1d 候选，不宣布胜过 G9 | **tile3 在 step ~8800 崩溃**（2026-09-11 06:29，`torch.AcceleratorError: CUDA error: unknown error`，发生在 `_grow_mipmap` 的 duplicate 阶段，人口 12.48M、cap 15M、崩溃前显存 14.0 GiB）。旧链在崩溃后仍按"latest.pt 存在"继续评估 step-5000 的中间 checkpoint 并准备合并——正是任务书 P0-1 描述的路径，已在合并前手动终止。崩溃目录保留为 `tile3_R1d_20k.crashed_step8800/`（日志在，3 GB 中间 checkpoint 已删）。按"OOM 重试改变配置即新臂"：tile3 以 `tile3_R1d_cap13m_20k`（仅 cap 15M→13M）重跑，交付改走加固后的 CLI（`eng` 分支 `ff7d6af`，`deliver R1d --tile0 tile0_R1_range0_20k --tile 3=tile3_R1d_cap13m_20k`，2026-09-11 07:20 起）：tile1/2 按完成判定复用，tile3 新训，合并 → merged.pt 评分 → 导出 → 0/0.01/0.05 阈值对照 → 回读导出 PLY 评分并绑定其 sha → 发布到 `exports/candidate_R1d/`。trainer 代码取 `eng` 工作树（= 研究基线，与 tile1/2 训练时一致），identity 写到 `quality_recovery_v2/identity/`。日志 `delivery_R1d/pipeline_cli.log` | `C:/Peter/3dgs-runs/house0305_sop/delivery_R1d/` |
| 队列里与任务书冲突的后续臂（B1 混杂、E1 仅加步数、P2 未做位姿依赖审计、tile1_T1a）已停用 | 已做：配置改名 `*.json.parked`，链在交付后自然结束 | — |
| WP00 运行身份 | 已出 | `00_runtime_identity.json`（含 gsplat commit/patch/扩展 hash、两个仓库 HEAD、正在运行的训练进程；注意 venv 跳板会让同一训练显示为父子两个 `train_gsplat` 进程） |
| WP00 CLI 四个 P0（完成状态机、配置冻结、最终 PLY 评分、原子 GPU 租约）+ 故障注入测试 | 已入库（`eng/cli-pipeline`，108 项测试）：`job_state.json` 状态机 RUNNING→CHECKPOINTED→TRAINING_COMPLETE→EVALUATED→QUALITY_ACCEPTED→PUBLISHED（FAILED / CONTROLLED_PAUSE 独立），完成判定 = checkpoint 可加载 + 步数达标 + 退出原因合法 + 时间戳新于作业；`config_frozen.json` 冻结，同名改配置拒绝；交付先评 merged.pt 再导出、按 0/0.01/0.05 出阈值对照、回读导出 PLY 评分并绑定其 sha，默认发布到 `exports/candidate_<TAG>/`；`gpu.lock` O_EXCL 租约（两进程竞争恰一个成功）。遗留臂（无 job_state）按日志+checkpoint 一次性"adopted"，这是有意的宽松，需要时可收紧；DA2/背景/AT 尚未接入租约 | — |
| WP02 日程审计（随附脚本） | 已跑：`audit_pack/` 8 项自测通过；对仓库配置与 as-run 各跑一次，**as-run sha 与仓库配置一致**（tile0 `40b4d71a…`、tile1 `7728ae57…`）。tile0 R1：135 次常规 refine、最后一次 13900、`late_prune_threshold_reachable=false`、末步名义 means LR 1.8454e-6 = 终值 1.6e-7 的 11.53×；告警 S001（20k 截断 42640 日程）、S002（21320 不可达）、G001（试 rgb_only 增殖信号）、E001（曝光无零均值/锚定）、A001（SH 从头全开） | `02_schedule_audit_pack_repo.json`、`02_schedule_audit_pack_asrun.json` |
| WP02 S0/S1 研究日程合同 | 已入库（`977fbce`）：`schedule_contract: research_rescaled_horizon_v1`（显式 opt-in，缺省行为字节不变；MipMap 门禁拒签合同配置，不伪造 parity；训练前写 `schedule_contract_as_run.json` 并进入 `trainer_config_sha256`）。S1 = tile0 R1 基础上 `max_steps 42640→20000`、去掉 controlled stop、`prune_switch 21320→10000`（晚期阈值首次可达，40 次）、`means_step_fraction` 显式 null、`learning_rates.means` 设为 S0 实际底数 2.4857e-5 使 LR 在 20000 真正到 ×0.01；refine 窗口 500–14000、reset 300、SH 间隔不变。S0 = 基础配置原样改名。27 项测试。**尚未上 GPU**：按任务书顺序等 WP03 诊断集，先小样 | `run_configs/house0305_tiles/v9/tile0_S{0,1}_*.json`、`tools/make_rescaled_schedule_config.py` |
| R1d tile3（cap 13M）结果 | 09:17 完成，状态机 EVALUATED：训练视角锐度 0.362（G9d tile3 0.300，+21%；参考同帧 0.734），离轨迹 0.344（G9d 0.298，+15%），PSNR vs 参考 −0.44 dB，人口 9.89M（峰 11.68M，峰值显存 9.86 GiB），27 次 cap 截断。合并 PASS：4 切片 32.15M → core-owner 26.71M。**battery 步失败**：`evaluate_probe_views` 按名字替换派生 v8 `face4_lidar_val` 路径，而该目录在当天早上的磁盘清理中被我按"配置未引用"误删；正在用 `build_face4_lidar_geometry.py` 重建（~10 min），之后从 battery 步续跑交付。四切片 R1 vs G9d 汇总：tile0 +11%/−2%，tile1 +19%/−26%，tile2 −19%/+14%，tile3 +21%/+15%（训练视角/离轨迹锐度） | `tile3_R1d_cap13m_20k/`、`delivery_R1d/` |
| **R1d 候选交付完成（10:17）** | 状态 DELIVERY_DONE，输出 `exports/candidate_R1d/`（未 publish）。最终分数绑定回读 PLY sha `bc0907f4…`（18.09M，导出阈值 0.05；合并前 26.71M）。同 6 帧训练视角锐度：F6 0.156 / G9 0.211 / **R1d 0.238**（参考 0.574，= 41%）；离轨迹锐度 ours/ref：0.425 / 0.469 / **0.514**；离轨迹 PSNR vs 参考 16.78 / 16.70 / 16.47（1/4 分辨率对 F6 配对赢 4/18，G9 为 8/18）；battery（48 张**训练视角**）：G9 19.49/16.83 → R1d 19.29/16.50，alpha_p05 0.754 → 0.817。导出阈值对照：0 → 26.71M、0.01 → 23.65M（去 3.06M）、0.05 → 18.09M（去 8.62M，32%）；导出前后（merged.pt vs 回读 PLY）指标差异 ≤0.01，即被 0.05 过滤掉的三分之一高斯对这些指标几乎无贡献（对 viewer 观感的影响待 C 口径核对）。**判读**：R1 配方换来锐度（训练视角 +13%、离轨迹 +10%），代价是与参考/照片的光度-几何一致性略降（PSNR −0.2 dB，配对赢减半）；按任务书不是产品通过，需 WP05/WP07 几何验证后再定。图：`figures/R1d_vs_G9_vs_ref_matched.png`。battery 步两次失败均因早晨清盘误删派生缓存（`face4_lidar_val`、`view_backgrounds_val`），已重建 | `delivery_R1d/delivery_report.json`、`scores.txt`、`battery_final.json` |
| 诊断队列 | 10:58 起经 CLI 队列串行：U0 室内/室外 → U1 → DIAG-40 R1 → G0 → G1（H=3000）。**CLI 第二个集成 bug**：诊断配置的 `output_dir` 在 `RUN/diag_v2/<region>/runs/<arm>`，CLI 假定 `RUN/<arm>`，于是训练成功（3000 步、`latest.pt`、`run_manifest.json` 都在）却被判 "checkpoint missing" → FAILED，评估未跑。修复：CLI 以臂配置的 `output_dir` 为准（`75e718d`）。第二次重启又暴露两个判定缺口：被收养的运行没有退出码，"training complete" 标记不被接受；且训练器只在自然完成时写的 `run_manifest.json`（`training.status COMPLETE`、`completed_steps`）没被读，而日志已被失败的再次启动覆盖。修复：run_manifest 作为最强完成证据，无退出码时也认完成标记（`a1de82f`，4 项测试）。**诊断臂第一个实测发现**：室外 5 视角臂（cap 沿用 15M）在 step 2000 就长到 14.5M 高斯（室外单视角 7.9M、室内单视角 3.0M、室内五视角 3.4M），显存 12.3 GiB、16 步/分——少视角拟合会把整个切片的 cap 填满，增殖信号不随视角数/覆盖调节（对应任务书 WP04 §7.3 的"局部预算"问题）。处置：该臂终止，剩余 7 臂改为 `cap_max = 切片初始化点数`（tile0 7.04M / tile1 3.42M），新名 `*_ci`，全部过 `TrainerConfig.validate`；队列重排为 [3 个已完成 U0/U1 补评估] → 室外 U1_ci → DIAG-40 R1_ci → G0_ci → G1_ci | `diag_v2/queue_cli.log`、`diag_v2/<region>/runs/` |
| **U0 单图拟合结果（13:03）** | 室内门叶单视角（`img_4a58d805`，支持率 0.38，4.1 m，H=3000，2.97M 高斯）：训练视角锐度 ours/photo **0.760**（该视角上参考 0.460，ours/ref 1.57）；室外砾石单视角（`img_39a2d47a`，7.95M 高斯）：**0.876**（参考 0.670，ours/ref 1.62）。对比全量训练：室内 0.10、室外 0.30。**判读**：给定单张照片，表示与投影/尺度/滤波/优化链能把室内门叶拟合得比竞品还锐——按任务书 WP03 表，室内的糊不来自投影、尺度、滤波或表示支持，而来自多视图叠加时的冲突（配准 / 曝光 / 遮挡-可见性）。下一步看 U1（3–5 视角）是否立刻变糊 | `diag_v2/*/runs/diag_*_1_R1/`、`figures/U0_single_view_fits.png` |
| **U1 室内五视角（13:05）** | 5 个匹配良好的视角（约 7 m，两台相机、5 个 rig 帧，3.39M 高斯）：训练视角锐度 **0.968**（这些视角上参考 0.919）。5 视角也不糊 → 多视图冲突不在"几个视角"层面出现，而是在数百视角 / 20k 日程（配准误差累积、跨时段曝光、遮挡-可见性、reset/增殖生命周期）下出现。下一个分界点：DIAG-40（40 视角，含 R1 / G0 / G1） | `diag_v2/indoor_door_leaf_Tile_1/runs/diag_indoor_door_leaf_Tile_1_5_R1/` |
| U1 室外五视角（cap 1.34×init，13:29） | 5 个 2.5–3.3 m 的砾石视角，9.34M 高斯：训练视角锐度 **0.557**（这些视角上参考 0.876，ours/ref 0.64）——与室内五视角（0.97）不同，室外近距离砾石 5 视角已经低于参考。砾石是高频随机纹理，5 个近视角之间的亚像素配准/曝光差异会直接抹平它；室内门叶是低频结构，对同样的误差不敏感。也就是说"多视图冲突"的门槛因纹理频率而异 | `diag_v2/outdoor_gravel_Tile_0/runs/diag_outdoor_gravel_Tile_0_5_R1_c134/` |
| **DIAG-40 室内 R1（13:46）** | 40 个视角（右 34 / 左 6，cap 1.34×init=4.58M，终态 4.42M，未触顶）：训练视角锐度 ours/photo **0.288**（这 6 个采样视角上参考 0.759，ours/ref 0.38）；离轨 18 视角 PSNR 14.79、ours/ref 锐度 0.357。与 U1 五视角（0.968 / 参考 0.919）对比，**糊在 5 → 40 视角之间出现**，但采样视角不同（U1 的 5 视角 ⊂ DIAG-40，采样面不同），配对读数待队列空闲后用 40 视角 checkpoint 在 U1 五视角配置上重渲。**出图判读**（`figures/DIAG40_indoor_R1_views.jpg`）：40 视角的失败形态不是均匀模糊，而是**悬浮半透明烟雾/色块挡在墙面和门叶前**（img_02167、img_8a5782 天空面、img_a4bc51 三视角明显；img_26e10 墙板反而比参考锐）——即视角间深度不一致产生的漂浮体。这与 WP05 的根因一致：未剔除隐藏点的 vis6 缓存把门后/窗后回波当可见表面给 alpha 地板，视角一多这些矛盾支持就在空中凝成半透明团。故 D-line 2×2（修复缓存 × 严格可见性）优先级高于 G0/G1。室外 DIAG-40 R1 训练中 | `diag_v2/indoor_door_leaf_Tile_1/runs/diag_indoor_door_leaf_Tile_1_40_R1_c134/`、`identity/diag_indoor_door_leaf_Tile_1_40_R1_c134.json` |
| **DIAG-40 室外 R1（14:11）** | 40 个砾石视角（20/20，cap 1.34×init，终态 9.29M）：训练视角锐度 ours/photo **0.270**（采样视角上参考 0.832，ours/ref 0.38）；离轨 PSNR 15.16、ours/ref 0.380。**逐帧（评分器同口径）**：近表面帧正常甚至优于参考——金属天花板 img_1857 ours/ref 1.64、近砾石 img_9294 0.64、远棚 img_6cda 0.56；拉低中位数的是 4 个天空/树枝/远处吉普帧（ours/ref 0.09–0.17，`figures/DIAG40_outdoor_R1_views.jpg`）：切片外没有高斯，天空和远景本应由背景穹顶承担，我们却在相机附近长出灰白烟雾去糊它们。5 视角臂里同类天空帧（img_03f3、img_2c48）已经是 0.20 → 这不是 40 视角新增的失败，而是 compare 抽帧（等步长）把区域外内容算进了区域指标。室内 40 视角逐帧则全部 < 1（0.23–0.87），5 视角 4/6 ≥ 0.9 → 室内的退化是真的。**处置**：(1) 区域指标改为只在 ROI 视角/ROI 裁剪上算（01_roi_registry），compare 抽帧对区域外天空帧不计入；(2) 配对渲染（40 视角 ckpt × U1 五视角配置）已排在链后自动跑；(3) 天空烟雾本身是另一条线索：背景穹顶与切片外内容的交接（WP04 §7 之外，待 D-line 后看是否随严格可见性消失） | `diag_v2/outdoor_gravel_Tile_0/runs/diag_outdoor_gravel_Tile_0_40_R1_c134/`、`identity/diag_outdoor_gravel_Tile_0_40_R1_c134.json` |
| DIAG-40 室内 G0（14:27） | G0 = R1 + `lifecycle_execution_order: post_optimizer_gsplat`（O0→O1，G1 的必要对照）：整板锐度 0.274（R1 0.288）、离轨 PSNR 14.78（14.79）、ours/ref 0.348（0.357）；**ROI 口径**（新 `tools/score_compare_roi.py`，只算门叶框）ours/ref 中位 0.529（R1 0.593，n=5）；终态 4.417M（4.422M），形态各分位一致。**判读**：生命周期执行顺序在 40 视角上无可分辨影响（差异在 ±3% 噪声带内），G1 的读数可直接对 R1/G0 判。同时给出 ROI 口径下的分界：室内 U1 五视角门叶 ROI ours/ref **1.015 / 0.931**（与参考持平），40 视角 ROI 0.59 → 门叶本身的退化在 5→40 视角之间发生，与整板读数一致；室外 ROI 采样帧太少（n=2），等链后固定视角的 ROI compare（每区 45–49 个含 ROI 的视角，`roi_compare_ids.json`） | `identity/diag_indoor_door_leaf_Tile_1_40_G0_c134.json` |
| DIAG-40 室外 G0（14:54） | 整板 0.268（R1 0.270）、离轨 PSNR 15.15（15.16）、ours/ref 0.377（0.380）、终态 9.293M（9.293M）、ROI（n=2）ours/ref 0.98（R1 0.92）。与室内一致：**生命周期执行顺序（O0→O1）在两区都无可分辨影响**，G1（rgb_only 增殖信号）直接对 R1/G0 判 | `identity/diag_outdoor_gravel_Tile_0_40_G0_c134.json` |
| DIAG-40 室内 G1（15:12） | G1 = G0 + `densification_gradient_source: rgb_only`（任务书 §7.1 增殖信号假设）：整板 0.265（R1 0.288 / G0 0.274）、离轨 PSNR 14.64（14.79 / 14.78）、ours/ref 0.326（0.357 / 0.348）、ROI ours/ref 0.460（0.593 / 0.529）、终态 4.408M、形态一致。**判读**：只用 RGB 梯度驱动增殖没有把室内门叶变锐，反而各指标略低（在噪声带边缘）；WP04 §7.1 假设在 40 视角上 = 反证。增殖信号不是室内糊的根因，与"少视角能拟合锐"一致——问题在多视角矛盾监督（D-line）与光度（X-line） | `identity/diag_indoor_door_leaf_Tile_1_40_G1_c134.json` |
| DIAG-40 室外 G1（15:42）；**G-line 结论** | 室外 G1：整板 0.268（R1 0.270 / G0 0.268）、离轨 PSNR 15.13（15.16 / 15.15）、ours/ref 0.376（0.380 / 0.377）、ROI ours/ref 0.969（0.92 / 0.98）、终态 9.292M，形态一致。**G-line 结论（两区 × 三臂，H=3000、40 视角）**：生命周期执行顺序（G0）与 RGB-only 增殖信号（G1）都不改变结果，室内还略负；任务书 §7.1"增殖信号被 LiDAR/几何项污染"的假设在小样上**反证**，不进入下一轮矩阵。链 v4 全部 10 臂完成（`queue complete`），GPU 交给自动排程：配对/ROI compare → X1 ×2 → D-line ×6 | `identity/diag_outdoor_gravel_Tile_0_40_G1_c134.json` |
| **DIAG-40 ROI 口径与配对渲染（15:57）** | 链后自动跑完：(a) **配对整板**（40 视角 checkpoint 渲染在 U1 五视角臂自己的 6 帧上）：室内 **0.316** vs 五视角臂同帧 0.968（参考 0.919）；室外 **0.265** vs 0.557（参考 0.876）→ 同一批视角上，40 视角训练比 5 视角糊 3×（室内）/ 2×（室外），退化是真的，不是抽帧差异。(b) **ROI-only**（每区 45–49 个含 ROI 的固定视角，只算 ROI 框）：ours/ref 中位 室内 R1 **0.566** / G0 0.501 / G1 0.493，室外 R1 **0.602** / G0 0.605 / G1 0.601（Q1–Q3 见 `07_diag40_roi_scores.md`）。**判读**：ROI 口径下室内外差距消失（都是参考的 ~0.6），"室内特别差"一半是整板抽帧里的天空/切片外内容；G0/G1 在 ROI 上室内还低 10%，坐实 G-line 无益。基线从此改用 ROI-all 的 ours/ref（n≈45–49，配对），噪声带待 D-line 的 D0f 对 R1 复跑给出。G0/G1 checkpoint 已删（identity 冻结） | `07_diag40_roi_scores.md` |
| **DIAG-40 室内 X1（16:13）与亮度混淆的发现** | X1（冻结的每相机时间曲线，manifest 记 `frozen: true`、曲线 sha `75a1e5d4…`）：整板原始锐度 0.206（R1 0.288）、离轨 PSNR 14.41（14.79）、ROI 原始 0.362（0.593）——看似大负。但 Laplacian 方差随全局增益平方缩放：R1 的 canonical 渲染比照片亮 **16%**（整板 luma 比 1.164），X1 为 0.997；把每块亮度归一到照片均值后（`score_compare_roi.py --match-brightness`），整板 R1 0.219 vs X1 **0.227**，ROI（n=5）ours/ref R1 1.000 vs X1 0.891。**判读**：X1 在锐度上中性（±5% 噪声带内），它修的是亮度偏置，不是糊；出图上悬浮烟雾原样存在（`figures/DIAG40_indoor_R1_vs_X1.jpg`）。**更重要的副产物**：此前所有 ours/ref 读数都带亮度项——参考在暗门叶 ROI 上比照片亮 3×（原始 ref/photo 2.2 → 归一后 0.73）。重算 ROI-all 亮度归一：室内 R1 **0.881** / G0 0.863 / G1 0.990，室外 R1 **0.595** / G0 0.597 / G1 0.597。即：H=3000/40 视角小样上，**室内门叶的纹理能量已接近参考（0.88–0.99），真正的缺口在室外砾石（0.60）**；室内剩下的是悬浮体/烟雾（Laplacian 看不见的错误）而不是纹理缺失。判臂口径自此固定为亮度归一 ROI-all 的 ours/ref + 出图；室外 X1 训练中 | `07_diag40_roi_scores.md`（含两张表）、`identity/diag_indoor_door_leaf_Tile_1_40_X1_c134.json` |
| DIAG-40 室外 X1（16:37）；**X-line 结论** | 室外 X1：整板原始 0.282（R1 0.270），亮度归一 **0.264 vs 0.263**，luma 比 1.041 vs 1.012，离轨 PSNR 15.12（15.16），终态 9.288M。**X-line 结论**：冻结的每相机时间曲线在两区都不改变锐度（亮度归一后 ±4%），只改 canonical 亮度（室内把 +16% 偏置压到 0）。任务书 WP06 的"跨时段曝光不一致导致糊"假设在 H=3000/40 视角小样上 = 反证；X1 保留为交付时的亮度口径选项（合并不再需要烘每切片中位 gain），不进入锐度矩阵。ROI 口径读数随 D-line 自动排程一起出 | `identity/diag_outdoor_gravel_Tile_0_40_X1_c134.json` |
| **DIAG-40 室内 D1f（16:58）** | D1f = 修复缓存 vis6f + `strict_visibility`（config 已核：两项都生效）：整板亮度归一 **0.227 vs R1 0.219**，原始 0.278（0.288），离轨 PSNR 14.71（14.79），终态 4.4217M（4.4220M），形态各分位一致，loss 0.1408（0.1409）。训练遥测：`lidar_alpha_support_fraction` 末步 0.699（R1 0.854）、alpha 项 0.0100（0.0144），alpha 权重 0.1 → alpha 项只占总 loss 0.7%。**出图**（`figures/DIAG40_indoor_R1_vs_D1f.jpg`）：六个视角逐帧几乎相同，img_02167 / img_8a5782 / img_a4bc51 的悬浮烟雾原位原样。**判读**：在 H=3000 / alpha 权重 0.1 的小样上，穿透回波的 alpha 支持**不是**悬浮体的成因——D-line 室内反证（D0f、D1 仍会跑完以闭合 2×2，但预期同样中性）。两点待核：(1) 训练器 strict 模式只砍了 18% 支持，而 WP05 审计的严格规则砍 74%（0.851→0.222）——两者定义不同，要对齐；(2) 烟雾的颜色是树枝/天空色，pitch_up 面里切片外的树/天空只能靠背景库解释，怀疑悬浮体是切片在替背景库"画树"——下一条最小验证是遮蔽切片外像素的 RGB 监督（F-line） | `identity/diag_indoor_door_leaf_Tile_1_40_D1_c134_v6f.json` |
| **悬浮体成因线索：背景库只有天空穹顶（17:15）** | 代码事实：切片训练的逐视角背景库 `tile_backgrounds_v9/Tile_*/background_manifest.json` 的 `dome_source` 是 `probes/sky_house0305.pt`（天空穹顶），损失是 `final = render + (1 − alpha) · backdrop`；渲染器 mask 只排除鱼眼圆外、面 FoV 外、低权重与行人。实测（`figures/backdrop_vs_photo_pitchup.jpg`，img_8a5782 pitch_up）：照片是屋檐 + 细密树枝 + 蓝天；**背景库是一片蓝/棕色块的糊穹顶，没有树**；我方渲染在同一位置长出棕色树状烟雾。也就是说切片裁剪内、无 LiDAR 几何的像素（树、远景、切片外内容）只能由切片自己的高斯去"画"，这些高斯落在任意深度并遮挡其他视角——与 DIAG-40 室内 img_02167 / img_8a5782 / img_a4bc51 的树色烟雾完全对得上，也解释了为什么 U0/U1（视角少、矛盾少）不糊而 40 视角糊。G/X/D 三线中性也与此一致：它们都没碰这个监督。**下一条最小验证 F1**：`rgb_supervision_mask: lidar_support`——光度损失只在膨胀 LiDAR 支持 ∧ 渲染 mask 内计算，切片外像素零梯度（子任务实现中：旋钮、契约、门禁拒签、测试、两区配置、监督像素占比估计）。若 F1 在亮度归一 ROI 与离轨 PSNR 上不降且烟雾消失，则升级到全场需要重建四切片背景库或把"其他切片 + 树"纳入背景 | `figures/backdrop_vs_photo_pitchup.jpg` |
| DIAG-40 室内 D0f（17:13）；**DIAG 尺度噪声带** | D0f = 修复缓存 vis6f + dilated（与 R1 只差几何 manifest 一个契约键）：整板亮度归一 **0.226 vs R1 0.219**（+3%），原始 0.285（0.288），离轨 PSNR 14.73（14.79，−0.06 dB），终态 4.4207M（4.4220M），alpha 支持率 0.853（0.854——膨胀把过滤掉的回波又填回来了，所以 dilated 模式下修复缓存基本不改支持面）。**用途**：D0f 与 R1 是"几乎同配置"的复跑，把它当 DIAG 尺度的噪声带：整板亮度归一 ±3%、离轨 PSNR ±0.1 dB。回看：G0 −2% / G1 +12%（整板）/ X1 +4% / D1f +4% 都在或贴着噪声带，没有一臂超出 | `identity/diag_indoor_door_leaf_Tile_1_40_R1_c134_v6f.json` |
| **F-line 实现与监督像素占比（`34593d6`）** | 新旋钮 `rgb_supervision_mask: all | lidar_support`（缺省 all 字节不变；契约只在非缺省时多一个 `loss_contract.rgb_supervision_mask` 键；门禁拒签）：L1 / SSIM / 梯度 L1 / LiDAR-RGB L1 / 训练 PSNR / DA2 深度项全部只在膨胀 LiDAR 支持 ∧ 渲染 mask 内计算，按监督像素数归一，零支持视角给图连通的零；遥测 `rgb_supervised_fraction`。8 项新测试，149 项训练测试通过（13 项失败与 HEAD 相同：CUDA 扩展、预设夹具、`-c134` 旧配置）。**CPU 估计（`tools/estimate_rgb_supervision_fraction.py`，r=24，真实 vis6 缓存）**：F1 只遮掉 2–4% 像素（室内 0.980 / 室外 0.960；烟雾所在视角 0.92–1.00）——因为面 LiDAR 缓存是全场的，**树也有回波**，F1 只遮天空。用现成的 `tile_ownership_masking`（0.5 m 盒外回波的 15 px 邻域从光度 mask 剔除）拆分：**室内只有 45% 的回波在 Tile_1 盒内（pitch_up 36%）**，F1 ∧ ownership 把室内监督占比压到 0.61（`img_8a5782::pitch_up` 0.32），ROI 仍 ≥ 0.91。于是排三臂：**F0** = R1 + ownership（现成旋钮，只剔树邻域）、**F1** = 只遮无回波像素（天空）、**F2** = F1 + ownership（只留切片内回波附近）。判据：亮度归一 ROI-all ≥ R1 − 3%、离轨 PSNR ≥ R1 − 0.1 dB、出图上烟雾消失；F0/F2 与 R1 的差若超过 D0f 给的噪声带（±3% / ±0.1 dB）才算数。队列在 D 线之后自动接（`after_dline_f1.cmd`：F2→F0→F1，两区，含 ROI compare） | `09_rgb_supervision_mask.md`、`09_supervision_fraction/`、`tests/test_rgb_supervision_mask.py`、`run_configs/house0305_tiles/diag_v2/*_F{0,1,2}_c134.json` |
| DIAG-40 室外 D1f（17:39） | 修复缓存 + 严格可见性：整板亮度归一 **0.265 vs R1 0.263**，原始 0.271（0.270），离轨 PSNR 15.04（15.16，−0.12 dB，贴噪声带边），终态 9.298M（9.293M），末步 alpha 支持率 0.607（R1 0.607）。与室内一致：**D-line 两区都中性**；穿透回波的 alpha 支持不是 40 视角糊/悬浮体的成因（在 alpha 权重 0.1、H=3000 的口径下）。剩余 D0f 室外与 D1 两区只为闭合 2×2 | `identity/diag_outdoor_gravel_Tile_0_40_D1_c134_v6f.json` |
| DIAG-40 室外 D0f（18:04） | 修复缓存 + dilated（对 R1 的"近复跑"）：整板亮度归一 **0.263 vs 0.263**，原始 0.269（0.270），离轨 PSNR 15.23（15.16，+0.07 dB），终态 9.293M（9.293M）。室外噪声带比室内更窄（锐度 ±0%、PSNR ±0.1 dB）。2×2 里只剩 D1 两区（旧缓存 + strict），预期中性 | `identity/diag_outdoor_gravel_Tile_0_40_R1_c134_v6f.json` |
| DIAG-40 室内 D1（18:20） | 旧缓存 + 严格可见性：末步 alpha 支持率 **0.451**（R1 0.854、D1f 0.699）——支持面砍了近一半，整板亮度归一仍是 **0.218 vs 0.219**，离轨 PSNR 14.79 = 14.79，终态 4.424M。alpha 支持在 0.45–0.85 之间怎么变都不影响结果：D-line 2×2 三格已中性，最后一格（室外 D1）只是形式闭合。也说明 alpha 地板在这个口径（权重 0.1）下对锐度/悬浮体没有杠杆——它在 20k 全量臂上"驱动 opacity"的历史结论（`alpha-floor-is-the-opacity-driver`）不能外推到 H=3000 诊断臂 | `identity/diag_indoor_door_leaf_Tile_1_40_D1_c134.json` |
| DIAG-40 室外 D1（18:47）；**D-line 结论** | 旧缓存 + 严格可见性：整板亮度归一 **0.262 vs R1 0.263**，离轨 PSNR 15.05（15.16），终态 9.303M。**D-line 2×2 闭合**（两区 × {旧, 修复缓存} × {dilated, strict}，8 格含 R1）：全部落在噪声带内（室内锐度 0.218–0.227、PSNR 14.71–14.79；室外 0.262–0.265、15.04–15.23），alpha 支持率从 0.45 到 0.85 都不改变结果，出图上悬浮烟雾原样。**判读**：任务书 §8.3"可见性近似/穿透回波"假设在 H=3000 / alpha 权重 0.1 的小样上反证；vis6 缓存修复本身仍然正确（穿透回波是错的监督），但它不是当前糊/悬浮体的杠杆。至此 G / X / D 三线 + 生命周期顺序都已反证，只剩 F 线（切片外内容的光度监督）与 P 线（位姿）。F 线六臂自动接上（F2→F0→F1，两区） | `identity/diag_outdoor_gravel_Tile_0_40_D1_c134.json` |
| **DIAG-40 室内 F2（19:53）** | F2 = ownership 遮蔽 + LiDAR 支持光度 mask：整板亮度归一 **0.183 vs R1 0.219（−16%）**，离轨 PSNR **13.98 vs 14.79（−0.8 dB）**，终态 4.473M（+1%），loss 0.1354（监督面变小）；训练 46.5 min（R1 14.4 min）——`tile_ownership_masks` 每步在 CPU 上算全分辨率膨胀，慢 3.2×。**出图**（`figures/DIAG40_indoor_R1_vs_F2.jpg`）：img_8a5782 pitch_up 的树色烟雾、img_02167 / img_a4bc51 墙前的悬浮体**原样存在**，门叶不更清。**判读**：把树/天空像素从光度监督里拿掉并没有让悬浮体消失，反而少了监督、整体更糊、新视角更差——"切片在替背景库画树"的假设至少在这个实现下**不成立**：悬浮体不是靠这些像素的 RGB 梯度维持的（无监督后它们既不被推走也不被剔除）。剩余解释：(a) 悬浮体来自初始化点/增殖后"没人管"的高斯，被 yaw 视角的监督放到错误深度；(b) 位姿/配准误差让不同视角把同一面墙放在不同深度（P-line）。下一条最小验证 **F3 = 直接去掉 pitch_up 面**（40 视角只留 yaw 面）：若悬浮体随天空视角消失即是 (a) 的数据侧证据；F0（只 ownership）/ F1（只天空）继续跑完作为对照 | `identity/diag_indoor_door_leaf_Tile_1_40_F2_c134.json` |
| F3 配置（`f96d230`） | `build_diagnostic_set.py` 新增 `--exclude-faces / --reuse-selection / --preset-name`（缺省字节不变，排除记录进签名的 selection），`make_diagnostic_arm_config.py` 新增 F3 变体与 `--cap-init-multiplier`；+7 测试。**F3 = 同样 40 张父图，去掉全部 pitch_up_56 面**：室内 113 → 73 视角（yaw−35 34 / yaw+35 32 / pitch_down 7），室外 151 → 115；cap、日程合同、vis6 缓存与 R1_c134 完全相同（4,579,208 / 9,440,001）。ROI 固定视角里室内有 5 个是 pitch_up 面（对 F3 是新视角），ROI compare 改用 `roi_compare_ids_f3.json`（室内 44 / 室外 45）。队列自动接在 F-line 之后（`after_fline_f3.cmd`）。判据同前：亮度归一 ROI ≥ R1 − 5%、离轨 PSNR ≥ R1 − 0.1 dB、出图上墙前烟雾是否消失。注意 H=3000 不变时每视角 epoch 数上升（室内 26.5 → 41.1） | `run_configs/house0305_tiles/diag_v2/*_F3_c134.json`、`03_diag_arms.md` §8 |
| **正式 20k 模型的悬浮/无支撑高斯定量（20:40，用户目视确认）** | 用户在 viewer 里看 `tile1_R1d_20k` 门叶区裁剪（opacity ≥ 0.05）：清楚处很清楚、室内好，但**大量悬浮杂乱实体、屋檐上长出成片凸起高斯去拟合蓝天、切片边缘差、切片外长出很多东西**。定量（到 Tile_1 初始化 LiDAR 点云 342 万点的最近距离，CPU KD-tree）：全模型 334 万高斯中 **34% 离最近 LiDAR 点 > 0.2 m、27% > 0.5 m、20% > 1 m**，可见（op ≥ 0.05）子集同比例；远离表面的可见高斯 77.8 万个，z 中位 2.44 m（贴表面的 0.31 m）= 屋檐/天空带；35% 的高斯在切片 `training_and_export_box` 之外；门叶区裁剪里 24% > 0.2 m，z > 3 m 的屋檐带 27% > 0.5 m。**判读**：这是正式模型里真实存在的失败形态（诊断臂只是把它放大了），机制与"背景库只有天空穹顶、切片自己去画天空/树"一致——高斯在没有几何支撑的空中生长且从不被清除。**S 线（表面锚定）**：训练器新旋钮 `surface_anchor_prune`——cull 事件时剔除离初始化 LiDAR 表面 > 0.3 m 的高斯（含切片盒外），并拒绝无支撑父本增殖；子任务实现中；今晚在 **正式 Tile_1 20k 尺度**跑 `tile1_S1_anchor_20k`（不再只靠诊断臂），判据：远离表面比例、viewer 目视、亮度归一 ROI、离轨 PSNR、形态 | 测量脚本见会话 scratchpad，工具化为 `tools/audit_surface_anchor.py`（子任务） |
| DIAG-40 F2 室外 + F2 两区 ROI（21:17）；**F 线队列改道** | 室外 F2：整板原始 0.278（R1 0.270），离轨 PSNR 14.98（15.16），训练 80 min（R1 25 min）。**ROI-all 亮度归一**：室内 F2 **1.004**（R1 0.881，+14%，超出室内 ±5% 噪声带）、室外 0.593（0.595）；ROI-U1 室内 0.963（0.813）、室外 0.543（0.549）。**判读**：F2 把光度监督收缩到 LiDAR 支持像素后，门叶这类有支撑表面的纹理能量升到与参考持平，但整板亮度归一 −16%、离轨 PSNR −0.8 dB、悬浮体原样——容量集中到表面、无支撑区域退化、新视角变差；室外无变化。不是可交付的方向，但说明"监督只给有支撑的像素"对表面本身有益，值得与 S 线（表面锚定剔除）合用而不是单用。F0/F1（各 1–3 h、只作机制对照）已从队列撤下（`night_plan.cmd` 在室外 F2 完成后停掉 F 线队列，清理未开始的 F0 目录），GPU 改为：正式 20k Tile_1 在 49 个 ROI 视角上的口径核对 → **S1 正式 Tile_1 20k** → F3 两区 | `07_diag40_roi_scores.md` 待追加、`identity/diag_outdoor_gravel_Tile_0_40_F2_c134.json` |
| **正式 20k Tile_1 在 ROI 口径下的读数（21:18）——诊断臂不是有效替身** | 用同样 49 个含 ROI 的视角、同样的亮度归一 ROI 评分，给正式 `tile1_R1d_20k`（1829 视角、20k 步）出图：ours/ref 中位 **0.366**（原始 ours/photo 0.219），而 40 视角 / 3000 步的诊断臂 R1 是 **0.881**。出图（`figures/door_roi_20k_vs_diag40.jpg`，照片 | 竞品 | 诊断臂 | 正式 20k）：正式模型在门叶前有一层灰黑色半透明雾，门格几乎不可辨；诊断臂能看见门格。**判读**：真正的退化发生在全视角 / 20k 日程，诊断臂只复现了一个温和版本（ROI 已接近参考）。这意味着今天在诊断臂上得到的 G / X / D / F "中性"结论**只能说明它们在 40 视角小样上不起作用，不能外推为"在正式训练上也无效"**——诊断臂的天花板太高，看不出差别。修正：实验单元改为**正式 Tile_1 全视角 20k 臂**（102 min/臂，可承受），诊断臂降级为机制探针；S1 已按此设计 | `tile1_R1d_20k/compare_roi_compare_ids*`、`figures/door_roi_20k_vs_diag40.jpg` |
| **K 线（天空特殊处理）立项（21:50，用户批准 SegFormer）** | 用户方向：无 LiDAR 处允许自由生长，**天空是特例**——现在的天空穹顶背景与照片冲突，高斯被逼去拟合天空。竞品对齐后在 Tile_1 盒内 208 万高斯只有 1.8% 离 LiDAR > 0.2 m（我们 34%），说明"空中无高斯"是结果不是手段。DA2 相对深度不能当天空 mask（实测标出的是随机远处云块，`da2_sky_mask_check2.jpg`）；渲染 mask 无语义类。决定：用 `nvidia/segformer-b4-finetuned-ade-512-512`（ADE20K sky=2，权重 NVIDIA 非商用许可，**只用于生成研究臂的监督 mask，权重不进产品**，已获用户批准）为 3536 张训练面生成天空 mask（CPU 后台，子任务 A）；训练器新增 `sky_supervision`（子任务 B）：天空像素上累计 alpha → 0、光度/单目深度项排除天空像素、天空区域不触发增殖；mask 腐蚀 + "窗内无 LiDAR 回波"双保险防玻璃/过曝墙被误标。首臂 **K1 = 正式 Tile_1 20k + sky_supervision**，与 S1（上界探针）、R1d 三者同口径比较：远离表面比例、亮度归一 ROI、离轨 PSNR、viewer 裁剪 PLY | 待：`tools/build_sky_masks.py`、`sky_mask_train.json`、`12_sky_supervision.md`、`tile1_K1_sky_20k.json` |
| 文献综述（`f806fce`，88 篇引用） | `11_floater_handling_survey.zh-CN.md`：**没有一篇把"到 LiDAR 点的固定米数距离"当训练期剪枝主机制**——距离只出现在软损失、增长门或加权阈值；硬空间规则只用于合并时的所有权裁决或有包围盒的物体。分块系统（VastGaussian / CityGaussian / H-3DGS / BlockGaussian）无一例外先给切片一个**块外内容的替身**（全场粗先验 / 邻块支架 / 天空盒），BlockGaussian 直接把"监督视角里有块外内容"命名为悬浮体成因。用 `render + (1−α)·backdrop` 合成的系统（Street Gaussians / OmniRe / Splatfacto-W / Urban RF）**都配天空/背景 alpha→0 损失**，我们缺的正是这一项。排序：① 归属遮罩 + 远景替身 ② 天空/背景 alpha 损失 ③ 只限增长不剪（S1 的父本门单独成臂）④ LiDAR 射线视线约束（strict 可见像素、ε 退火）⑤ 距离加权 opacity 衰减 ⑥ 贡献剪枝收尾。§7.2 逐条列出 0.3 m 硬剪会删掉的四类真实内容（玻璃、细结构、扫描时动态物、LiDAR 盲区）。**据此调整**：S1 只作上界探针；K1（②，显式 SegFormer 天空 mask）今晚；S2（③，只留增长门、关掉 cull）紧随；B 线（①，全场粗先验做背景替身）明天立项；任何臂都要把"悬浮体消失"和"真实内容消失"分开计数（RadSplat 的 max α·T 贡献量作审计） | `11_floater_handling_survey.zh-CN.md` |
| K1 / S2 落地（`9a05138` / `faa6d42`）与夜间排程 | **天空 mask**：`tools/build_sky_masks.py` + `cloudstudio_3dgs/data/sky_masks.py`（签名 manifest、fail-closed 校验、模型身份钉死 revision `2641fd1e…`、许可注记）；规则：短边 512 保比例整图推理、softmax sky(2) ≥ 0.5、双线性上采样到面分辨率 ∧ 面有效 mask；6 面烟测（`figures/sky_mask_smoke.jpg`）云和过曝天空都进 mask，树干/车棚顶/白铁皮屋顶正确排除；**细枝（< 5 px）会被并入天空**，所以它是"天空区域"而非逐像素——训练侧用腐蚀 8 px + 窗内 24 px 无 LiDAR 双保险。val 8 面已建；train 3536 面 CPU 后台生成（2.8 s/面，ETA ~00:30）。**训练器 `sky_supervision`**：天空像素累计 alpha→0（权重 0.5）、L1/SSIM/梯度/LiDAR-RGB/训练 PSNR/DA2 排除天空像素、增殖屏蔽（中心像素 ≥50% 观测落在天空的父本不得 clone/split）、契约仅启用时出键、门禁拒签；11 项测试。**S2** = `surface_anchor_prune` 新增 `cull:false`（只保留增长门，不删任何高斯，综述 §7.3 第③项）。夜间：S1 → F3 ×2（`night_plan2.cmd`）→ S1 ROI compare → K1（等 mask manifest 落地并 validate 通过）→ K1 ROI → S2 → S2 ROI（`night_plan3.cmd`）。三臂 + R1d 同口径：远离表面比例（`audit_surface_anchor.py`）、亮度归一 ROI-all（49 视角）、离轨 PSNR、viewer 裁剪 PLY | `12_sky_supervision.md`、`tile1_K1_sky_20k.json`、`tile1_S2_growthgate_20k.json` |
| **S1 上界探针结果（23:07）——硬剪是破坏性的** | `tile1_S1_anchor_20k`（正式 Tile_1 20k + 离 LiDAR 初始化面 > 0.3 m / 盒外一律剪 + 无支撑父本禁生，103 min）：终态 274 万高斯（R1d 334 万），离轨 PSNR **14.66**（R1d 15.16 口径不同帧，见 ROI 行），6 帧整板亮度归一锐度 **0.019 vs R1d 0.082**，末步 loss 0.353（R1d 0.301）。**出图**（`figures/tile1_R1d_vs_S1.jpg`）：天空视角变成大块色斑（穹顶露出、被剪掉的树和天空高斯换成零碎补丁），墙面视角（img_a415）整面糊成涂抹，室内视角尚可。门叶区裁剪里高斯数与 R1d 几乎相同（143 万 vs 143 万）——剪掉的不是门前的悬浮体，而是增殖又被剪的循环把容量搅乱了。**判读**：硬剪不但不是方案，连"上界"都不是——它同时删掉真实内容（树、玻璃反射、LiDAR 盲区）并把训练推向更差的局部解；与综述 §7.2 和用户的判断一致。S1 到此关闭；S2（只留增长门、不剪）与 K1（天空 alpha）才是有信息量的臂。审计：盒外占比 0.170（R1d 0.353） | `10_surface_anchor/tile1_S1_anchor_20k_audit.json`、`exports/inspect_diag/door_region_tile1_S1_anchor_20k_op05.ply` |
| **DIAG-40 F3（去掉 pitch_up 面，23:48）** | 室内（73 视角）：**ROI-all 亮度归一 1.122**（44 个含 ROI 的 yaw 视角；R1 同 44 视角 0.876），逐视角配对比值中位 **1.095、26/44 视角 F3 更好**（对 F2 1.113、38/44；对 G0 1.189、31/44）；ROI-U1 0.995（R1 0.813）；出图（`figures/DIAG40_indoor_R1_vs_F3.jpg` 第一行，同一面 img_02167::yaw_minus_35）R1 墙前的灰色烟雾在 F3 里消失。室外（115 视角）：ROI-all **0.599**（R1 0.595）、ROI-U1 0.572（0.549）——无变化。整板与离轨读数因视角集不同不可配对。**判读**：把朝天的面拿掉，室内门叶 ROI 上升到高于参考（+9.5% 配对，超出 ±5% 噪声带但幅度温和），墙前烟雾消失，室外砾石不动——**天空视角是室内悬浮体的来源之一**，与 K 线（天空 alpha→0、天空区域不增殖）的假设方向一致；F3 本身不是方案（丢掉了 35% 视角、屋檐/上部结构失去监督），K1 要在正式 20k 上验证同样效果能否不丢视角地得到 | `identity/diag_*_40_F3_c134.json`、`07_diag40_roi_scores.md` 待追加 |
| **K1 结果（02:38）——正式 Tile_1 20k + 天空 alpha 损失** | 与 R1d 同数据、同日程、同帧配对：**离轨 PSNR 16.18 vs 15.16（+1.0 dB）**，49 视角门叶 **ROI-all 亮度归一 0.453 vs 0.366（+24%）**，6 帧整板亮度归一 0.089 vs 0.082，终态 364 万（+9%），训练 120 min。**但**：远离 LiDAR 表面比例没有下降（>0.2 m 0.368 vs 0.338，盒外 0.398 vs 0.353），出图（`figures/tile1_R1d_vs_K1.jpg`）天空视角仍是雾 + 树色团、墙面视角仍有涂抹——**悬浮体本身没有被消掉**。遥测解释了为什么作用弱：天空 mask 经腐蚀 8 px + "24 px 内无 LiDAR 回波"守卫后，训练里每步只剩 rgb mask 的 **1.9%**（p90 5.7%，manifest 里天空占 21.8%）——朝天视角里树枝回波稀疏但遍布，24 px 膨胀几乎把整片天空排除；增殖屏蔽整轮只拦了 217 个父本。**判读**：K1 在机制只发挥 ~10% 的情况下已给出配对 +1 dB / +24%（两者都远超诊断臂噪声带，但 20k 尺度的复跑噪声未标定），方向对；下一步 **K2** = 同配置、腐蚀 4 px、守卫 6 px（估算见下），并排一条 **R1d 复跑**作为 20k 尺度噪声带 | `10_surface_anchor/tile1_K1_sky_20k_audit.json`、`exports/inspect_diag/door_region_tile1_K1_sky_20k_op05.ply`、`identity/tile1_K1_sky_20k.json` |
| **S2 结果（04:15）——只留增长门也有害；S 线整体关闭** | `tile1_S2_growthgate_20k`（不剪任何高斯，只禁止离 LiDAR 初始化面 > 0.3 m 的父本 clone/split）：终态 **263 万**（R1d 334 万、S1 274 万），6 帧整板亮度归一 **0.017**（R1d 0.082，与 S1 的 0.019 同级），离轨 PSNR 15.00（R1d 15.16）。出图 `figures/tile1_R1d_vs_S2.jpg` 与 S1 同形态：天空/树视角成块状色斑、墙面涂抹。**判读**：S1 的破坏主要不是"剪"造成的，而是**增长门**——树、天空方向、LiDAR 盲区的内容必须靠远离 LiDAR 的父本增殖来表达，禁掉它们就把这些视角交给了糊穹顶。这正是用户"无 LiDAR 处也要允许自由生长"的判断，也与综述 §7.2 一致。S 线（硬剪 / 增长门）两臂均为负，关闭；后续只走 K 线（天空特殊）与 B 线（块外替身） | `identity/tile1_S2_growthgate_20k.json` |
| **K2 结果（06:15）——天空 mask 加强版** | 腐蚀 4 px / 守卫 6 px：训练里天空像素占比每步 **5.6%**（K1 1.9%，p90 18.9%），天空 alpha 均值末段 0.015（天空基本透明了），增殖屏蔽 4986 个父本；终态 **455 万**（R1d 334 万，+36%），115 min。配对 R1d：离轨 PSNR **15.62**（R1d 15.16，K1 16.18），6 帧整板亮度归一 **0.107**（R1d 0.082，K1 0.089）。远离表面比例仍不变（>0.2 m 0.367）。**出图**（`figures/tile1_R1d_vs_K2.jpg`）：天空视角里穹顶透出来了（树形可辨），但出现**新的深色/橄榄色不透明团块悬在天空里**；墙面视角（img_a415）的涂抹与 R1d 一样。**判读**：天空 alpha 损失确实把天空交还给了穹顶（机制生效），指标略优于 R1d，但 (a) 门前/墙前的悬浮体不是天空像素驱动的——它们对 K 线不响应；(b) 天空里留下的团块是被 alpha 损失压不到的高斯（在 mask 外或 6 px 守卫内）；(c) 高斯数 +36% 说明排除天空监督后容量流向别处。K 线保留（对天空有效、指标不降），但不是悬浮体的答案；**下一条：F3@20k**——在正式 Tile_1 全视角上去掉 pitch_up 面，直接检验诊断臂里"去掉朝天面 → 门前烟雾消失"是否在正式尺度成立 | `10_surface_anchor/tile1_K2_sky_20k_audit.json`、`exports/inspect_diag/door_region_tile1_K2_sky_20k_op05.ply`、`identity/tile1_K2_sky_20k.json` |
| F3@20k 立项（06:50，`3eb7f6e`） | 要在正式 Tile_1 上去掉 pitch_up 面遇到训练器预检 `max_steps == 20 × 视角数`（1829 → 1278 视角，36580 ≠ 25560）；按视角数重推日程会改 LR 衰减/剪枝切换点（不再是 R1d 的单一改动）。改为训练器新旋钮 `holdout_face_ids`：整面视角离开训练集但仍计入 20× 视角的 epoch 基数（复用空间留出的记账），日程逐字节不变（R1d 契约 sha `eca85467…` 复核不变；F3 契约 `366fb644…` 只多 `view_sampling.face_holdout` 一键），每个剩余视角被看 15.6 次（R1d 10.9）。`tile1_F3_nopitchup_20k`（551 个 pitch_up 面留出，44 个 ROI 视角全在训练集）排在 R1d 复跑之后（`night_plan5.cmd`）。判据同 K 线；重点看墙前烟雾是否像诊断臂那样消失，以及屋檐/上部结构失去朝天监督后的代价 | `tests/test_face_holdout.py`、`tools/derive_tile_inputs_variant.py`（备用的签名派生路径） |
| **20k 尺度噪声带（08:11，R1d 复跑）与 K 线重判** | `tile1_R1d_rerun_20k`（配置逐字节相同、同 seed，只有 CUDA 非确定性）：离轨 PSNR **16.14 vs 15.16**（差 1.0 dB！），49 视角 ROI-all 亮度归一 **0.315 vs 0.366**（−14%），6 帧整板 0.076 vs 0.082（−7%），终态 331 万 vs 334 万。**结论**：正式 20k 臂的复跑噪声比诊断臂大得多——离轨 PSNR ±0.5 dB、ROI ±7%、整板 ±4%（单对复跑估计）。据此重判：K1 的"+1.0 dB"和 K2 的 +0.5 dB **都在噪声带内，不算数**；K1/K2 的门叶 ROI 0.453 / 0.465 相对两次 R1d（0.315–0.366）高 24–48%，K2 整板 0.107 相对 0.076–0.082 高 30–40%，**超出复跑带 3 倍以上，方向可信但幅度待第二次复跑确认**；S1/S2 的整板 0.019/0.017 远低于带，负结论不变。以后任何 20k 臂的赢面判定门槛：ROI ≥ +15%、整板 ≥ +10%，PSNR 只作否决项（−1 dB 以上才算变差） | `identity/tile1_R1d_rerun_20k.json` |
| **F3@20k 结果（10:11）——诊断臂的 F3 效应在正式尺度不成立** | `tile1_F3_nopitchup_20k`（551 个 pitch_up 面留出，日程与 R1d 逐字节相同；第一次因留出记账代码位置错误崩溃，修复后重跑）：44 视角 ROI-all **0.385**（两次 R1d 0.301 / 0.368，K2 0.470），逐视角配对：对 R1d 0.975（20/44 更好）、对 K2 0.836（6/44）；6 帧整板 0.099（R1d 0.076–0.082、K2 0.107），墙面帧 img_a415 0.037（R1d 0.023）；离轨 PSNR 16.45（带内）。**出图**（`figures/door_roi_R1d_K2_F3.jpg`，门叶 ROI 五视角 R1d | K2 | F3）：三臂门前都有同样的深灰半透明层，K2 略清（能看到门格），F3 不比 R1d 好。**判读**：去掉朝天面在 40 视角小样上消掉的烟雾，在正式 1278 视角上没有消——门前那层雾不是天空面造成的，也不响应 K/S/F 任何一条线。它在所有臂里位置、颜色一致 → 更像**数据侧的一致冲突**：候选解释是扫描时门的状态与拍照不同（台账 `scan-time-moving-objects`：LiDAR 看的是门后），门叶本身没有 LiDAR 点、门洞里却有室内回波，初始化/alpha 支持把室内内容放在门平面附近，光度再怎么监督都是两套真相的折中。用 ROI 盒粗查初始化点深度分布不能分辨（盒太深），需要按墙面法向做一次门平面切片的 LiDAR/照片一致性核查再下结论。**处置**：F 线（数据侧去视角）关闭；K2 保留为目前最好的正式臂（ROI 0.47 vs 0.30–0.37，整板 +30%，天空归穹顶），下一步 (a) 门平面一致性核查（CPU），(b) K3 = K2 + 门/动态区域的 LiDAR 支持屏蔽（若 (a) 证实），(c) B 线背景替身 | `identity/tile1_F3_nopitchup_20k.json`、`07_diag40_roi_scores.md` |
| **门平面一致性核查（10:50，`1a6c49c`）——门前灰雾的机制** | 墙面平面拟合 65,961 点 RMS 1.3 cm。门洞（0.875 × 1.975 m）内 38,185 个初始化点里**只有 41 个（0.11%）落在墙平面 ±10 cm**（旁边等高墙带 35.6%）；45% 在平面后 0.1–0.3 m（门框），**55% 在 0.3 m 以后**（p50 1.12 m，室内物体）；LiDAR 里门叶是**开着 101.7°** 的单块叶（57,403 内点，RMS 0.9 cm，玻璃格是洞）。照片：绝大多数视角门也是开的（LiDAR 叶投影与照片叶重合，`13_door_plane/overlay_*.png`），**只有 10 个 rig 帧（21 个样本，1.45–1.92 m 近距）门是关的**——冲突存在但只占门可见样本的 4%（像素权重 14%）。关键：门洞里真正的内容（远处室内、后门）**在 Tile_1 盒外**（缝隙区 34% 的回波深于盒边 2.65 m，未裁剪 LAS 在 −3.5 / −6.8 m 有后墙），盒内没有初始化点、背景库只是天空穹顶，而 6 px 膨胀的 alpha 地板仍覆盖 **99.9%** 的门洞像素要求不透明，光度又要求颜色 → 切片只能在门洞里捏出一层不透明的雾。这就是所有臂共有的门前灰雾，也是综述第①项"块外内容归属"在本场景的具体形态。**处置**：O 线 = 现成旋钮 `tile_ownership_masking`（盒外回波的 15 px 邻域退出光度 mask、alpha/深度支持只用盒内回波），先做**预计算缓存**（现算每步慢 3.2×），然后 O1 = R1d + ownership、O2 = K2 + ownership；另一探针 H1 = 留出那 10 个关门 rig 帧。`03_roi_provisional.json` 的门 ROI z 范围（1.6–3.2）漏了门洞下半（0.81–2.785），下轮修 | `13_door_plane_consistency.md`、`13_door_plane/stage*.json`、`13_door_plane/overlay_0_*.png` |
| O 线落地（11:40） | 预计算归属缓存 `tile_ownership_v9/Tile_1`（1829 视角 4.8 min、146 MB、签名绑定面缓存/渲染 mask/LiDAR/切片输入/0.5 m/15 px；与现算逐数组相同，样本加载 0.2–0.85 s → 0.13 s），训练器 `tile_ownership_cache_*` 字段，契约只多一个 `cache_manifest_sha256` 键；12 项新测试，88 项相关测试通过。**归属统计（Tile_1）**：合计 24.8% 的受监督 RGB 像素被判为块外邻域退出、40.6% 的回波在盒内；pitch_down 几乎全在盒内，pitch_up 退出 42%/盒内 29%；门叶 yaw 面退出中位 40%、盒内 54%（`img_9f8e6e67::yaw_plus_35` 退出 67.6%）。这印证了门洞/上部视角一半以上的监督像素本来就不该由 Tile_1 负责。排程：K3（跑中）→ **O1** = R1d + 归属 → **O2** = K2 + 归属（`day_plan2.cmd`）。风险：像 F2 一样，退出 25% 像素会让无支撑区域退化、新视角变差；判读以门叶 ROI + 出图 + 离轨 PSNR 否决项为准 | `cloudstudio_3dgs/data/tile_ownership_masks.py`、`tools/build_tile_ownership_masks.py`、`tile1_O1_ownership_20k.json`、`tile1_O2_ownership_sky_20k.json` |
| K3 结果（12:16）——K 线剂量响应封顶 | 完整 SegFormer 天空区（腐蚀 2 px、无 LiDAR 守卫）：天空像素占比每步 **21.4%**（K2 5.6%），天空 alpha 均值 0.0006（完全透明），终态 407 万。门叶 ROI **0.472**（K2 0.470，K1 0.453，R1d 0.30–0.37）——ROI 不再上升；6 帧整板 **0.053**（K2 0.107，R1d 0.076–0.082），离轨 PSNR **14.66**（否决线）。出图（`figures/tile1_K2_vs_K3.jpg`）：天空视角只剩糊穹顶 + 深色团块，树枝全部没了；墙面帧 img_a415 反而最清（0.045）。**判读**：天空 alpha 的剂量超过 K2 后，SegFormer"天空区域"里的细枝被一起压成透明，新视角与整板变差；门叶 ROI 已在 K1/K2 饱和。**K 线定点 = K2**（腐蚀 4 / 守卫 6）；天空里的深色团块在 K2/K3 都在，它们是 mask 之外（被标为树/其他）的"树替身"高斯，要靠 B 线（有树的背景替身）而不是继续加天空 alpha | `identity/tile1_K3_sky_20k.json` |
| **B 线设计（`a3216f0`）——块外内容替身** | 盘点：交付合并件只含各切片核心（构造上没有块外内容）；四个切片盒平铺整个 LAS 包围盒，竞品 2245 万高斯 **0 个**在盒并集之外——"没人拥有的内容"要按 LiDAR 支持衡量：竞品离最近 LiDAR 点 > 0.5 m 的只有 **1.26%**（28.4 万，树冠，z 中位 9.2 m；Tile_1 区域仅 1.7k），我方 R1d 导出 **3.48%**（63 万），Tile_1 区域 11.1 万 = 竞品的 66 倍 → 那是悬浮体不是树。照片像素只有 3.4% 落在所有切片裁剪之外 → 问题是逐切片过拟合 + 合并归属，不是"没被看到"。代码事实：背景库解码缓存无上限（Tile_1 13.5 GiB）；**训练契约不签背景库 manifest sha**（待补）。设计 B1：`backdrop(view) = render(穹顶 + 替身 \ Tile_1 训练盒)`，替身 = 三个邻切片 R1d checkpoint（去掉盒内 126 万行后 1857 万）+ 全场粗先验 B0（无切片配置、3536 面、LiDAR 2M 点初始化、cap 3M、10k 步，`TrainerConfig.validate` 通过——无切片路径仍活着）。工具 `tools/build_standin_backgrounds.py`（按切片裁剪相机原分辨率渲染、盒排除、opacity 地板、逐源曝光调和、签名 manifest + `standin` 溯源、`--plan-only`/`--limit`），13 项测试。GPU 顺序：B0 75–90 min → 替身烟测 24 面（**必须目视**）→ 全量 15–25 min → B1 = R1d + 替身 ~105 min → B2 = K2 + 替身 ~115 min；判读同 K/O 线 | `14_standin_backdrop_design.md`、`14_standin_backdrop/`、`house0305_global_coarse_B0_10k.json`、`tile1_B1_standin_20k.json`、`tile1_B1_standin_K2sky_20k.json` |
| **O1 结果（14:25）——归属遮罩是目前最强的门叶信号** | `tile1_O1_ownership_20k`（R1d + 缓存归属：盒外回波 15 px 邻域退出光度 mask，alpha/深度只用盒内回波；123 min）：49 视角门叶 **ROI 0.509**（R1d 0.301–0.368，K2 0.470，K3 0.472），逐视角配对：对 R1d 中位 **1.159、41/49 视角更好**，对 R1d 复跑 1.468（43/49），对 K2 1.089（34/49）；6 帧整板 0.060（R1d 0.076–0.082）——天空/树帧下降，**墙面帧 img_a415 0.050 = 所有臂最高**（R1d 0.023、K2 0.034）；离轨 PSNR 15.80（带内）；终态 **612 万**（+83%）。出图（`figures/tile1_R1d_vs_O1.jpg`）：墙板与窗框明显更清、门前雾变薄；代价是退出监督的块外邻域（树下地面、天空）变成色块、天空回到糊穹顶。**判读**：把"本切片不该负责的像素"从监督里拿掉，切片自己的墙面/门叶就变清——机制与门平面核查一致；但退出的 25% 像素没有替身就成了色块，所以 O 线必须与 B 线（替身背景）合体才能交付；O2（K2 + 归属）跑中 | `identity/tile1_O1_ownership_20k.json` |
| **O2 结果（17:02）——归属 + 天空 alpha** | `tile1_O2_ownership_sky_20k`（K2 + 缓存归属，152 min）：49 视角门叶 **ROI 0.543**（O1 0.509、K2 0.470、R1d 0.30–0.37）；6 帧整板 0.062（O1 0.060），墙面帧 img_a415 **0.055**（历次最高）；离轨 PSNR 14.88（贴否决线）；终态 **924 万**（R1d 334 万，+177%）。出图（`figures/tile1_O1_vs_O2.jpg`）：墙板/窗框与 O1 同级，天空回到穹顶但树替身团块仍在，退出监督的地面块状色斑仍在。**判读**：门叶 ROI 再进一步（O2 > O1 > K2 > R1d，三次叠加都在噪声带之外），但高斯数失控（归属和天空各自释放的监督压力全部流向增殖）、新视角掉到否决线，块外区域没有替身就是色块。O 线不再单独加码；下一步就是 B 线把替身背景补上（B1 = R1d + 替身、B2 = K2 + 替身），之后 B3 = O2 + 替身才是候选交付配置。同时要给增殖加回上限（cap 15M 显然太松，K2/O 线的 +36%/+177% 说明 cap 需要按切片实测定，见 `gsplat-rerun-noise-and-inert-cap`） | `identity/tile1_O2_ownership_sky_20k.json` |
| B0 全场粗先验 + 替身背景烟测（17:53） | `global_coarse_B0_10k`（无切片、3536 面、LiDAR 2M 点初始化、cap 3M、10k 步受控停止，49 min）：终态 240 万高斯；出图（`figures/B0_global_coarse_compare.jpg`）树、天空、车辆、邻屋都成形但糊——作替身够用，作交付不够。替身规划：三个邻切片 R1d checkpoint + B0 去掉 Tile_1 训练盒内 26 万行与 opacity < 0.05 后 **2043 万 + 穹顶 10 万**，逐源曝光调和（B0 gain 0.885）。烟测 24 面（`figures/B1_standin_smoke_photo_vs_backdrop.jpg`，左照片右替身）：朝天面里树枝与云在正确位置、邻屋/车棚/树在 yaw 面正确出现；切片自己的墙面/门洞区域在替身里是灰棕色雾（盒内内容已剔除，剩下的是盒边邻块和 B0 的糊）——这正是设计意图：切片高斯不透明处遮住它，门洞这种没有切片内容的地方由 B0 的粗室内代替"必须不透明"的要求。全量 1829 面渲染中（约 20 min），之后 B1 = R1d + 替身、B2 = K2 + 替身自动跑 | `identity/global_coarse_B0_10k.json`、`tile_backgrounds_B1/Tile_1/`（本地） |
| B1 结果（19:52，初判）——替身背景 | `tile1_B1_standin_20k`（R1d + 替身背景，114 min）：离轨 PSNR **16.91**（两次 R1d 15.16 / 16.14，K1 16.18——历次最高，超出 ±0.5 dB 带），6 帧整板 0.080（带内；注意 B 臂的 compare 用自己的替身背景合成，天空帧不与旧臂同口径），终态 **353 万（+6%，增殖受控**，对比 K2 +36%、O1 +83%）。出图（`figures/tile1_R1d_vs_B1.jpg`）：树/天空视角由替身接管，**之前的树色烟雾和团块没有了**；墙面视角仍糊。远离表面比例 **0.343（R1d 0.338，没降）**，盒外 0.290（0.353）——替身没有让空中高斯消失，只是让它们不再需要画树。**门叶 ROI 0.225 不可采信**：标准 ROI compare 用穹顶背景库合成，B1 训练时门洞靠替身的粗室内填补，换回穹顶就露底；已生成"替身背景评估配置"，B2 跑完后对 B1 / B2 / R1d 三臂做公平 ROI（`day_plan4.cmd`）。审计 `10_surface_anchor/tile1_B1_standin_20k_audit.json` | `identity/tile1_B1_standin_20k.json`、`exports/inspect_diag/door_region_tile1_B1_standin_20k_op05.ply` |
| **B2 结果（21:46，初判）——天空 alpha + 替身背景** | `tile1_B1_standin_K2sky_20k`（K2 + 替身背景，110 min）：离轨 PSNR **17.02**（历次最高；R1d 15.16 / 16.14，B1 16.91），6 帧整板 0.096（K2 0.107、B1 0.080；B 臂 compare 用替身背景合成），墙面帧 0.039（R1d 0.023），终态 **355 万（+6%）**——有了替身，天空 alpha 不再把容量推向增殖（K2 单用 +36%）。**远离表面比例 > 0.2 m：0.227（R1d 0.338，−33%）；> 0.5 m 0.173（0.268）；盒外 0.267（0.353）**——第一条让空中高斯真正减少的臂。出图（`figures/tile1_B1_vs_B2.jpg`）：树/天空视角最干净（树由替身给出、无烟雾无团块）；墙面视角仍糊但结构略多。门叶 ROI 公平口径（替身背景评估配置）与 B1、R1d 一起在 `day_plan4` 里出 | `identity/tile1_B1_standin_K2sky_20k.json`、`10_surface_anchor/tile1_B1_standin_K2sky_20k_audit.json`、`exports/inspect_diag/door_region_tile1_B2_standin_K2sky_20k_op05.ply` |
| **B 线判读（21:53）与 B3 立项** | 公平口径（替身背景评估配置）验证：R1d 0.367（穹顶口径 0.366）、B2 0.395（0.393）、B1 0.225（0.225）——**背景库不影响门叶 ROI 读数**，B1 的门叶退化是真的。汇总正式 Tile_1 20k 各臂：门叶 ROI **O2 0.543 > O1 0.509 > K3 0.472 ≈ K2 0.470 > K1 0.453 > B2 0.395 > S1/F3 0.39 > R1d 0.30–0.37 > B1 0.225**；远离表面 > 0.2 m **B2 0.227**（其余 0.34–0.37）；离轨 PSNR **B2 17.02 > B1 16.91 > K1 16.18 > R1d 15.2–16.1**；终态高斯 B1/B2 355 万（受控）、K2 455 万、O1 612 万、O2 924 万。**判读**：三根杠杆各管一段——归属遮罩管门叶/墙面（ROI），替身背景管空中高斯与增殖失控（远离表面 −33%、+6%），天空 alpha 在有替身时才不失控；单用替身（B1）反而让门叶更糊（替身给了切片"不画"的借口）。**B3 = O2 + 替身**（归属 + 天空 + 替身）是三者合体的候选交付配置，已在 GPU 上跑（~2.5 h，`day_plan5.cmd`）；判据：门叶 ROI ≥ 0.50、远离表面 ≤ 0.25、离轨 PSNR ≥ 16.5、高斯 ≤ 500 万、出图无烟雾且墙面比 R1d 清 | `07_diag40_roi_scores.md`、`tile1_B3_own_sky_standin_20k.json` |
| **WP05 §8.3 可见性/alpha 支持（`a252124`）** | 新旋钮 `lidar_alpha_support_mode: dilated | strict_visibility`（缺省字节不变）。DIAG-40 视角上：当前支持面积 室内 0.851 / 室外 0.790，严格规则只剩 0.222 / 0.423，**室内 74%、室外 46% 的现有 alpha 支持不过严格可见性**（门叶 ROI 0.996 → 0.045），主要在深度断边。**根因**：`build_face4_lidar_geometry.py` 的 warp 路径只在 manifest 里记 `visibility_cell_px=6`，没有做隐藏点剔除，穿墙/玻璃/门叶背面的 LiDAR 回波被 alpha 地板当成可见表面（探针图上宽松规则违反率 12.7–53.5%，真正应用 `visible_point_mask` 后为 0）。处置：修工具并重建 `face4_lidar_train_vis6f`（子任务进行中，~1.5 h CPU），之后 2×2 对照 {旧缓存, 修复缓存} × {dilated, strict} 在 DIAG-40 上跑 | `05_visibility_alpha_audit.md`、`05_visibility_alpha_overlays/`（PNG 本地） |
| **WP05 §8.3 修复与 vis6f 缓存（`d8ff1f7`）** | `build_face4_lidar_geometry.py` warp 路径现在在 z-buffer 胜者上跑 `visible_point_mask`（与直接路径等价），manifest 新增 `visibility_filter{requested, applied, candidates, kept, removed}` 与逐面 `visibility` 计数（旧缓存无此块，可区分）；`--verify-visibility` 抽样复核。重建 `face4_lidar_train_vis6f`：3536 面、39 min、4.4 GB，**42.6% 的栅格回波被判为穿透并剔除**（有效像素 959M → 551M；pitch_down 保留 0.80、pitch_up 0.54、yaw 0.57；DIAG-40 视角室内保留 0.627 / 室外 0.706）。复核：旧缓存 5+5 DIAG 面宽松违反率均值 8.6% / 9.7%（最大 24.8% / 32.0%），修复缓存全部 0.000。切片几何 `tile_face_lidar_v9f/Tile_{0,1}` 已建（仅审计工具消费）。**2×2 配置**：D0 = R1_c134（旧缓存 + dilated，已跑）、D1 = R1_c134 + strict（旧缓存）、D0f = `R1_c134_v6f`、D1f = `D1_c134_v6f`；D0→D0f 只差几何 manifest sha 一个契约键，D0f→D1f 只差 7 个 alpha 支持键；DIAG_40 的 tile_inputs 不绑几何 sha，视角集不变。**GPU 排程**（自动，`after_x1_dline.cmd`）：X1 队列结束 → X1 的 ROI compare → D1f/D0f（室内、室外）→ D1（两区）→ 六臂 ROI compare。磁盘 33 GB 空闲，六臂约 13 GB，判完即删 checkpoint | `05_vis6_cache_fix.md`、`tests/test_build_face4_lidar_geometry.py`（7）、`run_configs/house0305_tiles/diag_v2/*_D1_c134*.json`、`*_v6f.json` |
| WP06 曝光审计（`acc312d`） | 四个 R1 切片 checkpoint 的逐图 gain（索引 = 排序后的父图 id，重算的每切片中位数与 merge_report 一致到 2e-8）：中位 0.915，室内 log-σ 0.266 vs 室外 0.138；**同一张图在两个切片里学到的 gain 差 |Δlog g| 中位 0.18（室内 0.29）**，烘焙后不变——约一半的逐图变化是模型残差拟合，不是照片属性。canonical 渲染比照片亮 7–13%。同表面跨视角颜色离散（WP03 区域）：室内 CV 0.516 → 学到的 gain 0.435（−16%，oracle −54%）；室外 0.250 → 0.247（无效），但同一 gain 按相机×20 s 分块平均能 −11%。低维结构：相机×10 s 块 R² 0.43–0.53，逐相机滞后自相关 0.8–0.9。**X0** = 现状；**X1** = 每台物理相机的时间平滑 log-gain 曲线（≈10 s 节点）、软均值锚定、全场估一次后各切片冻结；不建议硬零均值 | `06_photometric_consistency.csv`、`06_colour_dispersion.csv`、`06_exposure_findings.md` |
| **WP06 X1 实现（`7680461`）** | `exposure_compensation.mode = per_image | camera_curve`（缺省 per_image 字节不变，契约只在非缺省时多键）：每台物理相机沿采集时间的分段线性 log-gain 曲线（10 s 节点，±ln2 钳位）、软每相机均值锚、相邻节点 L2；`frozen_curve` 时不建优化器，各切片共用同一条全场曲线。`tools/fit_exposure_curve.py` 从四个 R1 切片已学 gain 拟合（切片偏置作 nuisance、Huber IRLS）：884 图 / 221 s → 每相机 24 节点（48 参数 vs 884 逐图），edf 36.6；残差 RMS 0.151（仅切片偏置 0.195，R² 0.40；室内 0.205 / 室外 0.108）；留一切片能解释另一切片 10–42% 的 gain 方差。X1 配置（两区 DIAG-40，`*_40_X1_c134`）已过 validate，研究分支已并入 `eng`（`6fd11fa`，75 项 CLI/训练测试通过）。**GPU 排程**：链 v4 结束后先做配对渲染（40 视角 checkpoint 在 U1 五视角配置上重渲、评分），再跑 X1 室内/室外（`after_chain_x1.cmd`，pid 12656）。判读口径见 `06_x1_design.md` §4：X1 的 canonical 亮度与 X0 不同（室内左相机 6 图均值 −0.17），锐度比较需亮度归一 | `06_exposure_curve_scene.json`、`06_exposure_curve_fit.csv`、`06_x1_design.md`、`tests/test_exposure_curve.py`（13）、`tests/test_fit_exposure_curve.py`（5） |
| 存储清理（15:20） | C: 只剩 28 GB 时删除五个已判定、identity 已冻结（`../quality_recovery_v1/identity/`）的 Tile_0 臂的 `checkpoints/latest.pt`：C2_cap17m、L1_alpha005、C1_cap15m、R0_rngfix、G9_candidate_t0（合计 15.9 GB，日志/评估/配置保留）→ 43 GB。未动任何缓存、R1d 四切片 checkpoint、G9d 交付切片与合并件。诊断臂的 checkpoint 在 ROI compare 跑完后同样按此规则删 | — |
| CLI 首次真实目录运行 | 交付 R1d 走 `eng` CLI 第一次启动 7 s 失败：CLI 先把 `job_state.json`/`config_frozen.json` 写进训练输出目录，训练器拒绝非空目录。已改为旁路目录 `RUN/<arm>.pipeline/`（`253dd1c`，112 项测试），07:37 重新启动，租约 `gpu.lock` 由 tile3 训练持有 | `delivery_R1d/pipeline_cli.log` |
| WP02 日程审计（仓库内 `resolved_schedule`） | 已出（`825c54c`）：与随附脚本交叉一致（末步 LR 1.845e-6 / 11.53×，tile1 1.290e-6 / 8.06×）。**新发现**：`learning_rates.means=1.6e-5` 不是优化器实际底数——`scale_calibration.means_step_fraction`（默认 0.0032 × 参考尺度）在 `trainer.py:3588` 覆盖它，实际底数 2.49e-5（tile0）/ 1.98e-5（tile1），末步 2.87e-6 / 1.59e-6。事件：135 次 grow/cull、45 次 reset，最后一次出生 13900，之后每张图只剩 2.9–3.3 次访问。每切片 3 项不一致（截断日程、LR 底数、晚期阈值不可达），仓库配置与 as-run 零差异。S1 联动字段清单见提交说明；因训练器预检要求 `max_steps = 20×视角数`、`prune_switch = max_steps//2`，S1 需要一个显式命名的研究日程合同（子任务进行中） | `02_schedule_audit.json`、`02_schedule_events.csv` |
| WP01 评估协议 | 已出。**核验结论：48 个 battery 视角在 v9 划分下 0 个留出**——battery 从 v8 的 `face4_val`（90 张 val）取图，而 v9 手工划分把这 90 张全放进 train；G9/G9d/R1d 所有 battery 读数都是训练视角分数。验证图也参加了 independent-AT 联合位姿求解（`pose_policy = accepted_independent_at_all_images`），manifest 里"validation never entered BA"是旧文案。分组 split 提案 371/42/30 rig 帧（回环块优先、静止帧钉在 train、held-out 最近训练相机 p50 0.38 m），未写任何 split manifest；换 split 后必须重训一条控制基线。RenderSpec 缺口：评估器不传 `pinhole_rasterize_mode/with_ut`、不应用逐图曝光 gain（合并却烘焙了逐切片 gain）、裁剪面/像素中心/色彩空间未传播。30 个 ROI（6 类 × 5，只看照片）。阴性对照：往返工具已跑 SH 降级/平移/换背景/噪声底，其余待 GPU | `01_view_membership.csv`、`01_grouped_split_proposal.json`、`01_eval_protocol.json`、`01_roi_registry.json`、`01_evaluator_negative_controls.json` |
| WP01b RenderSpec 与曝光口径 | 已入库（`96538d8`）：`render_spec.py` 15 字段 + 指纹，五个评估器输出 `render_spec`；`_load_backend(..., honour_render_mode)` 可按训练配置应用 `pinhole_rasterize_mode/with_ut`（默认关，旧分数不动）；`pixel_centre / colour_space / clipping` 标为 unpropagated。**曝光事实**：训练器验证渲染不应用逐图 gain；`merge --harmonize-exposure` 把每切片 gain 中位数烘进 DC 带（G9 0.8985/0.9430/0.9375/0.9634）；评估器一律不应用 gain → 单切片 checkpoint 为 canonical、合并件为 per_tile_gain。A（Face4 针孔）与 B（原始鱼眼 UT）指纹必然不同，分数不可混报。另：`evaluate_probe_views --reference-ply` 不按 PLY 阶数覆盖 sh_degree（竞品 PLY 若带 SH 会被 DC 评），已记录未改 | `01_exposure_policy_audit.md`、`tests/test_render_spec.py` |
| WP03 室内诊断 | 已出（`03_indoor_diagnosis.md`）。室内门叶区 vs 室外砾石区（各 2000 个 LiDAR 采样、884 视角）：严格有效视角 98 vs 194，**5 m 内 31 vs 113**；严格遮挡率 0.77 vs 0.57；LiDAR 支持率 0.23 vs 0.43；亮度跨视角 CV 0.44 vs 0.23（17% 室内采样 <60 luma）；没有任何采样 <3 视角 → 不是采集瓶颈。切面缓存 = 一次双线性重采样（与新算 remap 差 ≤0.5 灰阶），**不是糊化来源**；门边缘原图 10–90% 宽度 3–4 mm。门锁边缘的 LiDAR 锚点–照片边缘偏移在 0.5 m 与 2.2 m 都约 10 mm（按米而非按像素缩放）→ 相机–LiDAR 配准弱支持。假设状态：近距离有效视角不足 / 可见性近似 / 光度不一致 = 支持；模糊 = 反证；配准 = 弱支持；增殖信号 = 未知。下一步小样：U0 单图拟合、U1 3–5 视角、X0/X1 低维光度、D0/D1 严格可见性 alpha 支持 | `03_observation_coverage.csv/.json`、`03_source_vs_face_crops/`、`03_crop_features.json` |
| WP03 诊断臂 | 已入库（`4057a99`）：`build_diagnostic_set.py`（按严格可见性选父图，派生并重签名的 tile 输入/几何 manifest，引用原 PLY/npz）、`make_diagnostic_arm_config.py`（H=3000 研究日程合同：refine 500–2100、prune switch 1500、5 次 reset、16 次 grow/cull、最后出生后 1000 步）；10 个配置全部过 `TrainerConfig.validate`，篡改 manifest 被拒。室内 U0 `img_4a58d805`（支持 0.38，4.1 m）、U1 五视角约 7 m、DIAG-40 右 34/左 6；室外 U0 `img_39a2d47a`（0.97，3.4 m）、U1 2.5–3.3 m、DIAG-40 20/20。**GPU 顺序（交付完成后）**：U0×2 → U1×2 → DIAG-40 R1×2 → G0/G1×2，每臂 ≤30 min，走 CLI 队列 | `run_configs/house0305_tiles/diag_v2/`、`03_diag_arms.md`、`3dgs-runs/house0305_sop/diag_v2/` |
| WP04 §7.1 `rgb_only` 增殖信号 | 已钉住：`densification_gradient.py` 纯 torch 助手（两次反向 = 一次总反向的叶梯度；means2d 增长梯度只含 RGB；absgrad 快照不被第二次反向覆盖；审计不推进 RNG；拓扑变化后梯度/Adam/血统对齐），11 项测试；梯度审计修正：LiDAR range/normal 项此前**漏乘阶段倍率**，现报告 raw/weight/stage/effective、各参数组与 means2d 范数、成对余弦，几何正则对 means2d 记 N/A。**G0/G1 对照必须放在 `post_optimizer_gsplat` 顺序上**（vendor 顺序在 `trainer.py:1287` 拒绝 rgb_only），即 G0/G1 同时也是矩阵里的 O0→O1；两臂都保持 `absgrad false`、`footprint_weighted`。另记：`footprint_weighted` 永远读 `.grad` 不读 `.absgrad`，O2（absgrad）臂设计时要处理 | `tests/test_densification_gradient_source.py` |
| WP10 house0614 子集 | 子集 manifest/masks/split 已生成（`eng` 分支 `eda063b`）；运行手册已出（`eng` 分支 `docs/2026-09-11_house0614子集运行手册.zh-CN.md`：23 步、8 步需 GPU、磁盘 57–75 GB）。**阻塞**：C: 仅 50 GB 空闲；Mask R-CNN 权重不在本机；时间同步审计需要一个已训练 checkpoint（GPU）且工具无 CPU 渲染路径（`time_sync/BLOCKED.json`）。GPU 全链验证等 house0305 质量门 | — |
| S-line 表面锚定剪枝 `surface_anchor_prune` | 已入库（未提交、未训练）：测量确认 tile1_R1d_20k 有 **34% 高斯离 LiDAR 初始化云 > 0.2 m**（27% > 0.5 m、20% > 1 m，z p50 2.46 m vs 近组 0.37 m，35% 在切片框外）；DIAG-40 R1 室内 7.0% / 室外 1.1%。机制：cull 事件上与 opacity/尺寸 cull 同一次 `remove` 剪掉 > 0.3 m（及外扩框外）的行、新生儿 100 步豁免、远父本不得 clone/split；契约只在 enabled 多键，门禁拒签；`tools/audit_surface_anchor.py` 出表；14 项测试。三份 S1 配置（`tile1_S1_anchor_20k`、两区 `diag_*_40_S1_c134`）过 validate，与基线只差 `strategy.surface_anchor_prune`；保持 `pre_optimizer_vendor`（纯父本遮罩无布局依赖）。**GPU 顺序**：DIAG-40 S1×2（各 ≤30 min）→ tile1_S1_anchor_20k，判读按 10 §6 | `10_surface_anchor_prune.md`、`10_surface_anchor/*.json`、`cloudstudio_3dgs/training/surface_anchor.py`、`tests/test_surface_anchor_prune.py` |

台账 `../quality_recovery_v1/01_first_batch_ledger.zh-CN.md` 与 `02_research_loop_plan.zh-CN.md` 中"锐度涨过噪声带即赢、两赢即全场交付"的机械规则自本日起废止，判臂按任务书 §12 的多指标、室内外双控、配对/多 seed 口径执行。
