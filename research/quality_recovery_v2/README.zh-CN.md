# quality_recovery_v2 — 按《澳洲B机_3DGS全链路审计与Agent执行任务书_2026-09-11》执行

任务书于 2026-09-11 由用户在会话中提供（连同 `3DGS_B_machine_audit_agent_pack_2026-09-11.zip`）。执行开始时（2026-09-11 06:10 AEST）桌面上已找不到这两个文件，任务书正文以会话中读取的内容为准；随附的 `audit_gs_config.py` 未取得，WP02 的日程审计改由仓库内 `cloudstudio_3dgs/training/schedule_audit.py` + `tools/audit_training_schedule.py` 实现（同时满足任务书要求的 `resolved_schedule` 纯函数）。若需用原脚本复核，请重新提供该 zip。

执行顺序遵循任务书：WP00 → WP01/WP02 → WP03 → WP04/WP05 → 依诊断选择 WP06/WP07 → WP08/WP09；WP10 的 CPU 工作并行。不一次性排满整卡。

## 与任务书对应的落地状态

| 项 | 状态 | 文件 |
| --- | --- | --- |
| 正在跑的 tile3（R1d 交付）不终止，完成后归档为 R1d 候选，不宣布胜过 G9 | **tile3 在 step ~8800 崩溃**（2026-09-11 06:29，`torch.AcceleratorError: CUDA error: unknown error`，发生在 `_grow_mipmap` 的 duplicate 阶段，人口 12.48M、cap 15M、崩溃前显存 14.0 GiB）。旧链在崩溃后仍按"latest.pt 存在"继续评估 step-5000 的中间 checkpoint 并准备合并——正是任务书 P0-1 描述的路径，已在合并前手动终止。崩溃目录保留为 `tile3_R1d_20k.crashed_step8800/`（日志在，3 GB 中间 checkpoint 已删）。按"OOM 重试改变配置即新臂"：tile3 以 `tile3_R1d_cap13m_20k`（仅 cap 15M→13M）重跑，交付改走加固后的 CLI（`eng` 分支 `ff7d6af`，`deliver R1d --tile0 tile0_R1_range0_20k --tile 3=tile3_R1d_cap13m_20k`，2026-09-11 07:20 起）：tile1/2 按完成判定复用，tile3 新训，合并 → merged.pt 评分 → 导出 → 0/0.01/0.05 阈值对照 → 回读导出 PLY 评分并绑定其 sha → 发布到 `exports/candidate_R1d/`。trainer 代码取 `eng` 工作树（= 研究基线，与 tile1/2 训练时一致），identity 写到 `quality_recovery_v2/identity/`。日志 `delivery_R1d/pipeline_cli.log` | `C:/Peter/3dgs-runs/house0305_sop/delivery_R1d/` |
| 队列里与任务书冲突的后续臂（B1 混杂、E1 仅加步数、P2 未做位姿依赖审计、tile1_T1a）已停用 | 已做：配置改名 `*.json.parked`，链在交付后自然结束 | — |
| WP00 运行身份 | 已出 | `00_runtime_identity.json`（含 gsplat commit/patch/扩展 hash、两个仓库 HEAD、正在运行的训练进程；注意 venv 跳板会让同一训练显示为父子两个 `train_gsplat` 进程） |
| WP00 CLI 四个 P0（完成状态机、配置冻结、最终 PLY 评分、原子 GPU 租约）+ 故障注入测试 | 已入库（`eng/cli-pipeline`，108 项测试）：`job_state.json` 状态机 RUNNING→CHECKPOINTED→TRAINING_COMPLETE→EVALUATED→QUALITY_ACCEPTED→PUBLISHED（FAILED / CONTROLLED_PAUSE 独立），完成判定 = checkpoint 可加载 + 步数达标 + 退出原因合法 + 时间戳新于作业；`config_frozen.json` 冻结，同名改配置拒绝；交付先评 merged.pt 再导出、按 0/0.01/0.05 出阈值对照、回读导出 PLY 评分并绑定其 sha，默认发布到 `exports/candidate_<TAG>/`；`gpu.lock` O_EXCL 租约（两进程竞争恰一个成功）。遗留臂（无 job_state）按日志+checkpoint 一次性"adopted"，这是有意的宽松，需要时可收紧；DA2/背景/AT 尚未接入租约 | — |
| WP02 日程审计（随附脚本） | 已跑：`audit_pack/` 8 项自测通过；对仓库配置与 as-run 各跑一次，**as-run sha 与仓库配置一致**（tile0 `40b4d71a…`、tile1 `7728ae57…`）。tile0 R1：135 次常规 refine、最后一次 13900、`late_prune_threshold_reachable=false`、末步名义 means LR 1.8454e-6 = 终值 1.6e-7 的 11.53×；告警 S001（20k 截断 42640 日程）、S002（21320 不可达）、G001（试 rgb_only 增殖信号）、E001（曝光无零均值/锚定）、A001（SH 从头全开） | `02_schedule_audit_pack_repo.json`、`02_schedule_audit_pack_asrun.json` |
| WP02 S0/S1 研究日程合同 | 已入库（`977fbce`）：`schedule_contract: research_rescaled_horizon_v1`（显式 opt-in，缺省行为字节不变；MipMap 门禁拒签合同配置，不伪造 parity；训练前写 `schedule_contract_as_run.json` 并进入 `trainer_config_sha256`）。S1 = tile0 R1 基础上 `max_steps 42640→20000`、去掉 controlled stop、`prune_switch 21320→10000`（晚期阈值首次可达，40 次）、`means_step_fraction` 显式 null、`learning_rates.means` 设为 S0 实际底数 2.4857e-5 使 LR 在 20000 真正到 ×0.01；refine 窗口 500–14000、reset 300、SH 间隔不变。S0 = 基础配置原样改名。27 项测试。**尚未上 GPU**：按任务书顺序等 WP03 诊断集，先小样 | `run_configs/house0305_tiles/v9/tile0_S{0,1}_*.json`、`tools/make_rescaled_schedule_config.py` |
| R1d tile3（cap 13M）结果 | 09:17 完成，状态机 EVALUATED：训练视角锐度 0.362（G9d tile3 0.300，+21%；参考同帧 0.734），离轨迹 0.344（G9d 0.298，+15%），PSNR vs 参考 −0.44 dB，人口 9.89M（峰 11.68M，峰值显存 9.86 GiB），27 次 cap 截断。合并 PASS：4 切片 32.15M → core-owner 26.71M。**battery 步失败**：`evaluate_probe_views` 按名字替换派生 v8 `face4_lidar_val` 路径，而该目录在当天早上的磁盘清理中被我按"配置未引用"误删；正在用 `build_face4_lidar_geometry.py` 重建（~10 min），之后从 battery 步续跑交付。四切片 R1 vs G9d 汇总：tile0 +11%/−2%，tile1 +19%/−26%，tile2 −19%/+14%，tile3 +21%/+15%（训练视角/离轨迹锐度） | `tile3_R1d_cap13m_20k/`、`delivery_R1d/` |
| CLI 首次真实目录运行 | 交付 R1d 走 `eng` CLI 第一次启动 7 s 失败：CLI 先把 `job_state.json`/`config_frozen.json` 写进训练输出目录，训练器拒绝非空目录。已改为旁路目录 `RUN/<arm>.pipeline/`（`253dd1c`，112 项测试），07:37 重新启动，租约 `gpu.lock` 由 tile3 训练持有 | `delivery_R1d/pipeline_cli.log` |
| WP02 日程审计（仓库内 `resolved_schedule`） | 已出（`825c54c`）：与随附脚本交叉一致（末步 LR 1.845e-6 / 11.53×，tile1 1.290e-6 / 8.06×）。**新发现**：`learning_rates.means=1.6e-5` 不是优化器实际底数——`scale_calibration.means_step_fraction`（默认 0.0032 × 参考尺度）在 `trainer.py:3588` 覆盖它，实际底数 2.49e-5（tile0）/ 1.98e-5（tile1），末步 2.87e-6 / 1.59e-6。事件：135 次 grow/cull、45 次 reset，最后一次出生 13900，之后每张图只剩 2.9–3.3 次访问。每切片 3 项不一致（截断日程、LR 底数、晚期阈值不可达），仓库配置与 as-run 零差异。S1 联动字段清单见提交说明；因训练器预检要求 `max_steps = 20×视角数`、`prune_switch = max_steps//2`，S1 需要一个显式命名的研究日程合同（子任务进行中） | `02_schedule_audit.json`、`02_schedule_events.csv` |
| WP01 评估协议 | 已出。**核验结论：48 个 battery 视角在 v9 划分下 0 个留出**——battery 从 v8 的 `face4_val`（90 张 val）取图，而 v9 手工划分把这 90 张全放进 train；G9/G9d/R1d 所有 battery 读数都是训练视角分数。验证图也参加了 independent-AT 联合位姿求解（`pose_policy = accepted_independent_at_all_images`），manifest 里"validation never entered BA"是旧文案。分组 split 提案 371/42/30 rig 帧（回环块优先、静止帧钉在 train、held-out 最近训练相机 p50 0.38 m），未写任何 split manifest；换 split 后必须重训一条控制基线。RenderSpec 缺口：评估器不传 `pinhole_rasterize_mode/with_ut`、不应用逐图曝光 gain（合并却烘焙了逐切片 gain）、裁剪面/像素中心/色彩空间未传播。30 个 ROI（6 类 × 5，只看照片）。阴性对照：往返工具已跑 SH 降级/平移/换背景/噪声底，其余待 GPU | `01_view_membership.csv`、`01_grouped_split_proposal.json`、`01_eval_protocol.json`、`01_roi_registry.json`、`01_evaluator_negative_controls.json` |
| WP01b RenderSpec 与曝光口径 | 已入库（`96538d8`）：`render_spec.py` 15 字段 + 指纹，五个评估器输出 `render_spec`；`_load_backend(..., honour_render_mode)` 可按训练配置应用 `pinhole_rasterize_mode/with_ut`（默认关，旧分数不动）；`pixel_centre / colour_space / clipping` 标为 unpropagated。**曝光事实**：训练器验证渲染不应用逐图 gain；`merge --harmonize-exposure` 把每切片 gain 中位数烘进 DC 带（G9 0.8985/0.9430/0.9375/0.9634）；评估器一律不应用 gain → 单切片 checkpoint 为 canonical、合并件为 per_tile_gain。A（Face4 针孔）与 B（原始鱼眼 UT）指纹必然不同，分数不可混报。另：`evaluate_probe_views --reference-ply` 不按 PLY 阶数覆盖 sh_degree（竞品 PLY 若带 SH 会被 DC 评），已记录未改 | `01_exposure_policy_audit.md`、`tests/test_render_spec.py` |
| WP03 室内诊断 | 已出（`03_indoor_diagnosis.md`）。室内门叶区 vs 室外砾石区（各 2000 个 LiDAR 采样、884 视角）：严格有效视角 98 vs 194，**5 m 内 31 vs 113**；严格遮挡率 0.77 vs 0.57；LiDAR 支持率 0.23 vs 0.43；亮度跨视角 CV 0.44 vs 0.23（17% 室内采样 <60 luma）；没有任何采样 <3 视角 → 不是采集瓶颈。切面缓存 = 一次双线性重采样（与新算 remap 差 ≤0.5 灰阶），**不是糊化来源**；门边缘原图 10–90% 宽度 3–4 mm。门锁边缘的 LiDAR 锚点–照片边缘偏移在 0.5 m 与 2.2 m 都约 10 mm（按米而非按像素缩放）→ 相机–LiDAR 配准弱支持。假设状态：近距离有效视角不足 / 可见性近似 / 光度不一致 = 支持；模糊 = 反证；配准 = 弱支持；增殖信号 = 未知。下一步小样：U0 单图拟合、U1 3–5 视角、X0/X1 低维光度、D0/D1 严格可见性 alpha 支持 | `03_observation_coverage.csv/.json`、`03_source_vs_face_crops/`、`03_crop_features.json` |
| WP03 诊断臂 | 已入库（`4057a99`）：`build_diagnostic_set.py`（按严格可见性选父图，派生并重签名的 tile 输入/几何 manifest，引用原 PLY/npz）、`make_diagnostic_arm_config.py`（H=3000 研究日程合同：refine 500–2100、prune switch 1500、5 次 reset、16 次 grow/cull、最后出生后 1000 步）；10 个配置全部过 `TrainerConfig.validate`，篡改 manifest 被拒。室内 U0 `img_4a58d805`（支持 0.38，4.1 m）、U1 五视角约 7 m、DIAG-40 右 34/左 6；室外 U0 `img_39a2d47a`（0.97，3.4 m）、U1 2.5–3.3 m、DIAG-40 20/20。**GPU 顺序（交付完成后）**：U0×2 → U1×2 → DIAG-40 R1×2 → G0/G1×2，每臂 ≤30 min，走 CLI 队列 | `run_configs/house0305_tiles/diag_v2/`、`03_diag_arms.md`、`3dgs-runs/house0305_sop/diag_v2/` |
| WP04 §7.1 `rgb_only` 增殖信号 | 已钉住：`densification_gradient.py` 纯 torch 助手（两次反向 = 一次总反向的叶梯度；means2d 增长梯度只含 RGB；absgrad 快照不被第二次反向覆盖；审计不推进 RNG；拓扑变化后梯度/Adam/血统对齐），11 项测试；梯度审计修正：LiDAR range/normal 项此前**漏乘阶段倍率**，现报告 raw/weight/stage/effective、各参数组与 means2d 范数、成对余弦，几何正则对 means2d 记 N/A。**G0/G1 对照必须放在 `post_optimizer_gsplat` 顺序上**（vendor 顺序在 `trainer.py:1287` 拒绝 rgb_only），即 G0/G1 同时也是矩阵里的 O0→O1；两臂都保持 `absgrad false`、`footprint_weighted`。另记：`footprint_weighted` 永远读 `.grad` 不读 `.absgrad`，O2（absgrad）臂设计时要处理 | `tests/test_densification_gradient_source.py` |
| WP10 house0614 子集 | 子集 manifest/masks/split 已生成（`eng` 分支 `eda063b`）；运行手册已出（`eng` 分支 `docs/2026-09-11_house0614子集运行手册.zh-CN.md`：23 步、8 步需 GPU、磁盘 57–75 GB）。**阻塞**：C: 仅 50 GB 空闲；Mask R-CNN 权重不在本机；时间同步审计需要一个已训练 checkpoint（GPU）且工具无 CPU 渲染路径（`time_sync/BLOCKED.json`）。GPU 全链验证等 house0305 质量门 | — |

台账 `../quality_recovery_v1/01_first_batch_ledger.zh-CN.md` 与 `02_research_loop_plan.zh-CN.md` 中"锐度涨过噪声带即赢、两赢即全场交付"的机械规则自本日起废止，判臂按任务书 §12 的多指标、室内外双控、配对/多 seed 口径执行。
