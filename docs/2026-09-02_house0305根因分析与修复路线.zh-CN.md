# 2026-09-02 house0305 根因分析与修复路线（接手复盘）

本文是接手后的第一份全面分析：11 个维度的独立审计 + 32 条对抗复核（代码重读 / 遥测复算 / 影响评估三视角），全部落在真实代码行与真实检查点数字上。此前所有在 reset=300 时代得出的结论已作废，不再引用。

## 一、现状一句话

**h5v 基线（reset 3000、means lr 1.6e-5、immediate cull、逐切片裁剪背景）跑满 5 个 epoch 后，形态和 P10 尾部都在恶化，不是在收敛。**

切片级电池（Tile_0 自己的 48 个裁剪训练视角，四个模型同一组视角、同一背景、同一掩膜）：

| 模型 | 步数 | 高斯数 | PSNR 均值 | **P10** | alpha 均值 | **alpha P05** |
|---|---:|---:|---:|---:|---:|---:|
| 参考交付 PLY | — | 22.45M（全场景） | 15.54 | **13.37** | 0.876 | **0.500** |
| resetB3000 探针 | 3,500 | 5.10M | 15.79 | 12.07 | 0.785 | 0.314 |
| h5v 预 reset 检查点 | 12,000 | 13.69M | 15.69 | 11.25 | 0.793 | 0.328 |
| h5v 受控停点 | 12,390 | 8.82M | 15.46 | 11.31 | 0.780 | 0.304 |
| 旧 cadence 交付切片 | 1,850 | 2.50M | 15.34 | 11.64 | 0.726 | 0.250 |

形态（对数尺度约定，参考值在括号内）：

| | 初始化 | 3,000 | 7,434 | 12,000 | 12,390 | 参考 |
|---|---:|---:|---:|---:|---:|---:|
| 最长轴 p50 mm | 7.07 | 13.85 | 18.89 | 23.80 | 22.41 | **4.41** |
| 最短轴 p50 mm | 3.54 | 1.57 | 0.81 | 0.65 | 0.66 | **0.43** |
| opacity p50 | 0.10 | 0.37 | 0.66 | 0.73 | 0.57 | **0.197** |
| opacity > 0.9 占比 | — | 0.16 | 0.28 | 0.34 | 0.16 | 0.02 |
| 中心最近邻 p50 mm | 4.47 | 0.93 | 0.44 | — | — | 1.5–2.7 |
| 1 mm 格内共址占比 | 0.005 | 0.33 | 0.61 | — | — | 0.055 |

读法：训练越久，长轴越长、opacity 越饱和、堆叠越密，P10 越差。均值 PSNR 略升是因为大而不透明的高斯把中频"糊"对了；尾部视角（朝上看树冠、朝下看白背景）在变坏。图像对比也一致：树冠区域是天空色的不透明糊团，砾石是条纹状"刀片"，参考是干净的照片感。

停点位置（reset+390 落在冲刷谷底）只影响数量和均值，**不影响 P10 尾部**（12,000 vs 12,390：11.25 vs 11.31），这条此前的 P0 猜想降级为 P2。

## 二、根因排序（已确认 = 代码行 + 遥测/检查点复算一致；假设 = 机制成立但影响量未测）

### R-A【已确认，P0】监督信号被深度噪声主导，RGB 只占总 loss 的 9%

- 我方总 loss 中 LiDAR range 项占 62–71%、DA2 占 18–27%、RGB（L1+DSSIM）约 9%（h5v 遥测 928 步复算，range 项在 73% 的步是最大加权项）。
- range 项是 **0.5 × 线性米制 L1**，而训练器自身默认是 **0.05 × robust log-Huber**（trainer.py 默认值），切片配置在 3a06caa 静默覆盖，无任何记录理由。
- 它永远拟合不上：range L1 分窗中位数从 2.7 m 到 3.8 m 全程不降。原因在数据侧：**face4 LiDAR 缓存没有遮挡剔除**，Tile_0 有 57% 的 LiDAR 监督像素落在可见表面之后（穿墙点、树冠后的点）；参考交付件对同一批目标打出 4.86 m MAE，说明目标本身不可拟合。
- 致密化梯度源是 total_loss，所以这份噪声还决定了克隆在哪里出生。
- 参考配方（逆向恢复）没有稀疏 LiDAR range 项，深度监督走近全像素 DA2/mesh 且在有界压缩空间。
- DA2 项自身也有缺陷（P2）：mono_depth_mask 没有远距截断，对齐后的天空目标达到千米量级（fp16 上限），线性 L1 让 da2 loss 中位 2.6 m、最大 116 m。

### R-B【已确认，P0】opacity 体制反了：只有向上推力，没有向下排水

- 我方 alpha 地板（weight 0.5、target 0.95、6 px 膨胀）覆盖 76–95% 的掩膜像素，**包括枝叶之间的天空**，是单向 relu(0.95−α)² 的推力；参考配方里没有这一项。
- 除 reset 外没有任何向下的力：opacity_sparsity 0、scale_upper 0，各向异性/切向/压平只作用于轴比。
- 结果：每个 reset 周期内均值 opacity 单调上升 0.31→0.63，三分之一高斯超过 0.9；reset 钳到 0.2 后 100 步的 immediate cull 一次清掉 35–36%（3101/6101/9101 三次实测），形成锯齿。参考人口在 0.05 硬地板之上单调递减，p50 0.197，只有 2% 超过 0.9。
- 树冠"白絮"的直接机制就在这里：地板把枝间天空排到 α≥0.95，唯一同时满足地板和照片的解是天空色的不透明高斯（APP-1）。
- 复核员的修正：仅加 0.01 的 mean-opacity 正则在当前体制下不够，地板才是主驱动；所以阶梯先撤地板，再加排水。

### R-C【已确认，P0】没有任何缩小算子，克隆偏向大而不透明的父本

- split 在构造上不可达：split 门槛 0.2 m 与 world shrink 上限 0.2 m 同值，173 次 refine 事件 split 恒 0（h5v/resetA/resetB 全部）。
- clone 是逐位复制：子代出生在父本原位，Adam 状态清零；means lr 1.6e-5（绝对米制，无场景尺度乘子）每步最多 0.01 mm，一个周期内分不开。7434 步时 61% 高斯与另一个共享 1 mm 格，中心最近邻 p50 0.44 mm，而长轴 18.9 mm——**用堆叠而不是铺展来覆盖**。
- clone 的选择有尺寸偏置：可克隆父本长轴 p50 29.5 mm（人口 18.9）、opacity p50 0.91（人口 0.66）。3.6 次人口周转后长轴被棘轮式推高。
- 压平项在 tangent_ratio 模式下的分母不 detach，梯度同时压短轴、**拉长切向轴**；参考自身厚度比 0.18，我方目标 0.1 是过度压薄。
- 参考的长轴 4.41 mm 恰好 = 初始化 7.07 mm ÷ 1.6，即一代 split 的结果；参考最近邻 1.5–2.7 mm。
- 可走的路：detail split 策略（校验器钉死 0.02 m / 0.0035 屏幕半径、revised_opacity 必须为 true），7434 步时 47.5% 的高斯长轴超过 0.02 m，split 分支第一次有父本可用。

### R-D【已测量，未复核】切片外监督泄漏

- 裁剪矩形只按像素裁，不按切片归属掩膜：Tile_0 有 33% 的 LiDAR 监督像素（Tile_1 63%、Tile_2 48%）的三维点在切片盒之外，那里没有高斯、背景是白色，RGB/range/alpha/DA2 目标在构造上不可满足；唯一能降低 loss 的自由度是盒内高斯沿射线长大变实。
- 后果是切片边界的"墙"：盒边 0.2 m 带内长轴 p50 130 mm、opacity 0.94，合并时又被 core-owner 丢掉。

### R-E【已测量，未复核】评测口径

- 原 48 视角电池全部在 Tile_1 内（时间块 0），不能给 Tile_0 打分；包含天空；朝下视角背景纯白且 LiDAR 支撑 0.09%。
- 本次已加 `--tile-views`（切片自己的裁剪训练视角）和前景掩膜/分面统计，作为切片级 A/B 的判据；它是训练视角分数，必须如实标注。真正的保留集需要空间分块划分，属于下一次数据集重建。

### 被复核推翻或降级的

- "init opacity 0.1 落在 cull 门上导致第 500 步砍掉 55%"：数字属实，但改成 0.2 不改变均衡（cull 是持续流，不是一次性事件）。
- "reset+100 冲刷占每周期死亡的 70–78%"：实测只占 21–26%，稳态 cull 才是大头。
- "停点落在冲刷谷底是 P0"：对 P10 无影响（见上表）。
- "期望深度 vs 中值深度导致长轴拉伸"（LOSS-04）：机制未测量，撤回。
- "加 0.01 mean-opacity 正则能把 opacity 拉回参考"：在地板未撤时无效。

## 三、实验谱系（显式血统；每臂 3,500 步 = 一个完整 reset 周期 + 冲刷 + 400 步恢复；以 `ladder_lineage.json` 为准）

```
h5v
└─ R1 range 0.05 robust
   ├─ R2 去 alpha 地板                       [判负：alpha P05 −0.09，opacity 不动]
   └─ R3 adaptive-detail split，地板保留      [胜]
      ├─ R3B 去 alpha 地板                   [交互检查：P10 +0.1，覆盖塌]
      ├─ R4 flatten 绝对 1 mm                [判负：短轴 1.14→3.33 mm]
      └─ R5 统一 0.05 cull                   [胜；R5r 为显式血统下的复现]
         ├─ R6 opacity 排水 0.01             [判负：人口减半、alpha P05 −0.1]
         ├─ C1 切片归属掩膜                   [监督正确性]
         ├─ C2 DA2 远距截断 + 压缩空间        [监督正确性]
         ├─ C3 隐藏点剔除 LiDAR 缓存          [监督正确性，缓存已重建：train 剔 52%]
         │  └─ C4 = C3 + C1 + C2             [正确性合集]
         ├─ T1 split 门 10 mm / T2 5 mm      [尺寸机制；将改挂到 C3/C4 赢家之下]
         └─ S1 尺寸铰链（中位初始尺寸 2 倍）   [尺寸机制；同上]
```

执行顺序：C1 → C2 → C3 → C4 → R5r；C3/C4 定出赢家后，T1/T2/S1 从赢家重新派生再跑（不在脏监督上比尺寸）。每臂产出：形态六指标、切片级电池（P10、alpha P05、前景 P10）、3101 冲刷比例、split 血统（父本/子代 opacity 与尺寸按年龄分桶、人口增速）。5 mm 门一旦出现人口爆炸或离面人口上升即停。

**不开 20 epoch 长跑**：血统探针已证明 20 mm 门下多跑代数不会让尺寸穿过 12–20 mm 平台，长训只是在这一层做人口交换。

### 阶梯实测（2026-09-02 中午，切片级电池，同一组 48 个裁剪视角）

| 级 | 高斯数 | 短轴 | 长轴 p50 / p95 | opacity p50 | op<0.1 / >0.9 | PSNR | P10 | alpha | alpha P05 | 判定 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 参考 | 22.45M | 0.43 | 4.41 / 55.8 | 0.197 | 18% / 2% | 15.54 | 13.37 | 0.876 | 0.500 | 目标 |
| resetB3000（h5v 配方 @3500） | 5.10M | 1.42 | 14.8 / 119 | 0.475 | 6% / 13% | 15.79 | 12.07 | 0.785 | 0.314 | 基线 |
| R1 range 回默认 | 3.60M | 1.32 | 12.6 / 140 | 0.567 | 3% / 16% | 16.45 | 12.41 | 0.839 | 0.416 | **胜**，六项全向参考走 |
| R2 撤 alpha 地板 | 3.27M | 1.22 | 12.5 / 125 | 0.558 | 3% / 16% | 16.34 | 12.34 | 0.767 | 0.330 | **负**：alpha P05 −0.09，opacity 纹丝不动，地板不是饱和驱动 |
| R3 = R1 + split 可达 | 4.69M | 1.14 | 13.5 / **55** | 0.561 | 4% / 15% | **17.11** | 12.76 | **0.885** | **0.517** | **胜**：alpha 均值与 P05 越过参考；split 首次发生（229 万父本） |
| R6_A = R3 + flatten 绝对 + cull 0.05 + 排水 0.01 | 3.12M | **4.19** | 11.9 / 60 | **0.223** | **23% / 2%** | 16.97 | **12.88** | 0.842 | 0.431 | opacity 分布与参考对齐；短轴变厚归咎 flatten 绝对模式（米制平方梯度≈0） |

**审稿修正（2026-09-02 下午）**：第一版 stager 是累积 mutate，被判负的臂会隐式带进后面的臂；已改为显式血统（每臂声明 base + 单一改动，`ladder_lineage.json` 记录 base 与配置 sha，运行目录保存 `config_as_run.json`）。按实际运行的遥测核对：上表 R3 的确是"R1 + split、alpha 地板保留"（遥测里 alpha loss 存在），R5 是"R3 + cull 0.05、flatten 仍为 tangent_ratio"，R6 是"R5 + 排水 0.01"；只有 R6_A 是混合臂（带 flatten 绝对模式）并已如此标注。补做 R3B（R3 去 alpha 地板）回答 R2 与 split 的交互。

**结论措辞修正**：R2 的单变量证据只说明 alpha 地板撑住了覆盖（alpha P05 −0.09）而 opacity 分布未动，**不能**说地板是 opacity 饱和的主驱动；它确定的问题是污染覆盖语义（枝间天空被排成不透明）。R3 的 split 是 CloudStudio 自己的 adaptive-detail split（0.02 m 屏幕半径门），不是参考配方的复刻，参考的 type-2 路径是 clone 为主、世界尺度大才 split。

**过程事故**：detail split 强制的 revised_opacity 在库函数里对饱和到 1.0 的父本给出 +inf logit，两条探针分别在 2800/2900 步被 fail-closed 拦下；已在适配器侧夹紧修复（03ca89e），带回归测试。

### R3B 与血统探针（2026-09-02 下午）

R3B = R3 去 alpha 地板：PSNR 16.76（R3 17.11）、P10 12.86（12.76）、alpha 均值 0.701（0.885）、alpha P05 0.382（0.517）、opacity p50 0.557（不变）、长轴 p95 35.5 mm（55.2）。结论：地板不驱动 opacity 饱和（两次单变量都不动），但它撑住覆盖和均值，同时把长轴尾部吹大、把 P10 尾部（树冠/天空视角）压低 0.1 dB。保留地板，后续用"天空掩膜排除地板 + 缩小膨胀半径"去争这 0.1 dB，而不是整个撤掉。

血统探针（R3B @3500，按出生方式/年龄分桶）：

| 人口 | 数量占比 | opacity p50 | >0.9 | 长轴 p50 | 短轴 p50 |
|---|---:|---:|---:|---:|---:|
| 初始化存活 | 47% | 0.558 | 12% | 10.2 mm | 1.10 |
| clone 子代（100–300 步龄） | 8% | 0.653 | 30% | 14.6 | 0.81 |
| clone 子代（1000–3000） | 11% | 0.556 | 13% | 13.4 | 0.97 |
| split 子代（100–300） | 8% | 0.594 | 24% | **18.8** | 1.06 |
| split 子代（1000–3000） | 8% | 0.588 | 14% | **18.1** | 1.23 |

split 父本的 opacity 从第 501 步的 p50 0.29 一路升到 2900 步的 0.93（>0.9 占 58%），reset 后回落再爬升；子代出生后 opacity 随年龄回落到人口水平，没有停在 0.999——数值修复之外没有"饱和子代"堆积。**真正的问题是尺寸**：split 子代出生时就 18–19 mm（父本 30–45 mm ÷ 1.6），之后不再变小，而校验器把 detail split 门钉在 20 mm，18 mm 的子代永远不再分裂。人口长轴中位数被 12–20 mm 这一层锁住，与"更多代数自然变锐"的假设相反。参考的 4.4 mm = 初始化 7 mm 一代 split，参考从没让高斯长到 30 mm。

因此新增三臂（叠在 R5 上）：T1/T2 把 detail split 门放到 10 mm / 5 mm（校验器与 gate 改为接受 {0.02, 0.01, 0.005}）；S1 在中位初始尺寸的 2 倍处加尺寸铰链（scale_upper 0.01、ratio 2）直接对抗长大。

### 正确性臂与尺寸臂实测（2026-09-02 傍晚，全部显式血统、同一评估版本）

| 臂 | base | N | 长轴 p50 / p95 | 短轴 | opacity p50 | PSNR | P10 | alpha | alpha P05 | 判定 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| R5 | R3 | 6.75M | 12.5 / 52 | 1.18 | 0.414 | 17.18 | 12.99 | 0.888 | 0.541 | 基线 |
| R5r（复现） | R3 | 6.74M | 12.5 / 52 | 1.18 | 0.417 | 17.19 | 13.15 | 0.891 | 0.534 | 与 R5 吻合；**噪声下限 P10 ±0.16、alpha P05 ±0.01** |
| C1 归属掩膜 | R5 | 7.21M | 12.7 / 49 | 1.19 | 0.415 | 16.81 | 12.91 | 0.819 | 0.452 | 负；只评本切片像素时仍负（P10 14.28 vs 14.48） |
| C2 DA2 截断+压缩 | R5 | 6.20M | 12.3 / 51 | 1.15 | 0.423 | 17.25 | 13.05 | 0.913 | 0.555 | 小幅全正向；前景池化 alpha P05 0.005→0.44 |
| C3 隐藏点剔除缓存 | R5 | 7.11M | 12.3 / 50 | 1.18 | 0.420 | 17.20 | 12.94 | 0.888 | 0.533 | 中性（0.05 robust 下穿透目标梯度本就≈0） |
| T1 split 门 10 mm | R5 | 7.25M | **9.9** / 50 | **0.99** | 0.417 | 17.14 | 12.79 | 0.882 | 0.534 | 尺寸按预期下降、人口 +7%；电池在噪声内 |

边界墙直接量（盒边 0.2 m 带）：h5v 时 130 mm / opacity 0.94，R5 已是 11.9 mm / 0.45（与内部一致）——"墙"是旧 loss 体制的症状，R1/R3/R5 已消除，C1 只再削 10% 盒外高斯，却因 15 px 膨胀丢掉边界附近的有效监督。

**口径事实**：只评本切片像素（`--tile-owned`）时，R5 P10 14.48 / alpha P05 0.644，参考 12.87 / 0.519。全裁剪电池里我方落后参考的 P10 全部来自切片外像素（参考有全场景内容），要靠三切片合并解决；单切片真正落后的是尺寸与未测的保留集泛化。

T1 血统：split 子代出生尺寸 18–19 mm → 12 mm，split 352 万次（R3 230 万），opacity 分布不变，无人口爆炸。

### 夜间队列实测（2026-09-02 晚，C2 基线的尺寸臂 + 保留集臂 + C5）

训练视角电池（同一评估版本；噪声下限 P10 ±0.16）：

| 臂 | base | N | 长轴 p50 / p95 | 短轴 | PSNR | P10 | alpha P05 | 判定 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| C2 | R5 | 6.20M | 12.3 / 51 | 1.15 | 17.25 | 13.05 | 0.555 | 基线 |
| C4 合集（含 C1） | C3 | 7.37M | 12.5 / 45 | 1.17 | 16.61 | 12.55 | 0.422 | 负（C1 的代价），留证据 |
| T1c 10 mm | C2 | 6.61M | 9.8 / 49 | 0.97 | 17.15 | 12.72 | 0.532 | 尺寸降，P10 −0.33 |
| **T2c 5 mm** | C2 | 6.69M | **9.3** / 48 | **0.89** | 17.19 | 13.07 | 0.537 | **尺寸降、P10 持平、人口 +8%、盒外 +13%** |
| S1c 尺寸铰链 | C2 | 6.79M | 12.2 / **24.5** | 1.28 | 16.97 | 12.80 | 0.469 | 负：只压尾部，中位不动，开洞 |
| **C5 去稀疏 range** | C2 | 6.55M | 12.1 / 49 | 1.16 | 17.16 | **13.15** | **0.579** | 略优于 C2；门的糊团减轻但未消 |

保留集 v1.1（同一组 301 个被扣视角，1.5 m 隔离带，最近训练相机 p50 1.9 m；训练视角 1613/2478，每视角约 2.2 次曝光）：

| 臂 | 保留 PSNR | 保留 P10 | 保留前景 P10 | 保留 alpha P05 | 训练视角 P10（同臂） |
|---|---:|---:|---:|---:|---:|
| R5h | 14.87 | 11.24 | 11.17 | 0.390 | 13.0（R5） |
| C2h | **14.91** | **11.39** | 11.27 | **0.444** | 13.05 |
| S1h | 14.75 | 11.34 | 11.31 | 0.366 | 12.80 |

**训练视角 → 保留视角的差距约 2.3 dB（P10 约 1.7 dB）**，这是第一次拿到真实空间泛化数字；C2 的改进在保留集上成立，S1 在保留集上也判负。T2h 因磁盘满失败，已清理 36 GB 后重跑；C5h 在跑。

血统（T2c）：split 子代 8–9 mm，初始化存活者 10.3 mm 占 48%——初始化点长大后的尺寸成了新地板；铰链压不动它而不开洞，split 路线是唯一同时缩小又补覆盖的机制。

候选配方：**C5 + 5 mm split**（C5T2），3,500 步有/无保留集各一臂，之后延长到 7,000 步看两个 reset 周期后的走势。

## 四、代码修复（本分支 fix/loss-parity-and-split，默认关闭、不改变任何已签名配置的行为）

1. `mono_depth_max_range_m`（新配置键）：DA2 掩膜远距截断，建议 30 m。
2. `da2_depth_space: "compressed"`（新配置键）：DA2 残差在有界压缩空间计算，与 mesh 项一致。
3. 电池：`--tile-views`、前景掩膜 PSNR/P10、池化 alpha P05、分面（yaw/pitch_up/pitch_down）统计。

4. `DepthProjectionConfig.visibility_cell_px`（新字段，默认 0）：face/鱼眼两条投影路径的隐藏点剔除——每个点与其 3×3 粗格邻域最近距离比较，超过 (1+容差)×最近 + 边距即剔除；启用后缓存清单记录 `_visibility_filtered` 后缀与 `projection_config`。这是 R-A 的根治，但要重建 face4_lidar_train/val 与 DA2 对齐（数据集签名会变，gate 链需重跑）。
5. `tile_ownership_masking`（新配置键，默认关闭）：数据集加载时把 LiDAR 返回反投影到世界系，落在切片训练盒（+边距）之外的返回从深度掩膜移除；其膨胀邻域（且不靠近本切片返回）从 RGB/单目掩膜移除——R-D。无需 sidecar、无需重建数据。阶梯 R7 试它。

排队中：
6. 空间分块验证集（SplitConfig spatial_block 2 m）——R-E，需重训。
7. 若 R3 不足以让克隆分开：clone 子代协方差采样偏移（clone_offset_sigma）或 clone 子代缩放 1/1.6。
8. 天空掩膜排除 alpha 地板（若 R2 撤地板后 alpha P05 塌陷而必须保留地板时才需要）。

## 五、交付定义

- 三切片各跑满 20 epoch 的赢家配置，core-owner 合并（保留），导出 PLY。
- 门槛（切片级电池 + 全场景保留集）：P10 ≥ 参考 −0.5 dB、alpha P05 ≥ 0.45、前景 P10 ≥ 参考 −0.5 dB、LPIPS 不高于参考 +0.02；形态：长轴 p50 ≤ 6 mm、短轴 ≤ 0.6 mm、opacity p50 ∈ [0.15, 0.35]、opacity>0.9 ≤ 5%。
- 人眼检查：树冠无白絮、砾石无条纹、切片缝无墙。

## 六、优先级重排（2026-09-02 傍晚，按第三轮审稿）

- **门的案例（2026-09-02 晚）**：室内帧里那扇关着的白门渲染成半透明糊。LiDAR 深度图在门的位置看到的是门后 4.7 m 的树——扫描时门是开的，照片里是关的；初始化在门的位置没有点，稀疏 range 项把高斯往 4.7 m 拉，光度项要在 1.5 m 画门，两者打架。参考配方没有稀疏 LiDAR range 项，所以它跟着照片。长跑修不好这类"扫描时的动态物体"，只有去掉/门控稀疏 range 项才能修：新增 C5（C2 + range 权重 0）与 C5h。C3 的隐藏点剔除对此无效（门在 LAS 里根本不存在）。
- **P0 空间保留集（当前为 spatial-cell holdout v1.1，不是完整 spatial-block validation）**。现有 48 视角保留集全在 Tile_1；切片臂的分数全是训练视角。已加不动签名清单的版本：`holdout_spatial_cell_m` / `holdout_fraction` / `holdout_seed` 按 2 m 相机格整图扣掉 10% 的切片视角不参与训练（记录 `holdout_views.json`），电池用 `--holdout-from` 只评这些视角。臂：R5h、C2h、T2h、S1h、C5h（同一组保留视角）。v1.1 已按审稿补齐：1.5 m 隔离带（隔离带视角既不训也不评）、记录被扣相机到最近训练相机的 min/p05/p50/p95 与相邻格数、实际扣除比例、曝光账目（签名步数按切片全视角计，剩余训练视角实际约 22 个 epoch，h 臂之间可比，但 R5h 对 R5 的差不能全归泛化）。**已知泄漏**：每视角背景是用全部帧（含被扣帧）烘焙的天空穹顶，所以当前是"表面高斯保留集"，主判据用前景掩膜 P10 与 alpha P05；完整版要在 spatial_block 数据集重建时把背景/骨架一起按 split 隔离。
- **P1 尺寸控制**：C2 基线上的 T2（5 mm 门）与 S1（尺寸铰链），分别回答"长大后拆"与"一开始别长大"；5 mm 门必须同时看人口增速、子代出生偏移、离面人口。
- **P1 三切片合并电池**：全裁剪 P10 的差距来自切片外内容，只有合并模型能回答。
- **P2** C3（数据正确性保留，非画质杠杆）、C1（当前 15 px 版本过激，不入主线）。
- **不做**：为 histogram parity 强压 opacity（R6 0.01 已判负）；20 epoch 长跑。
- 术语：10/5 mm adaptive-detail split 与尺寸铰链是 CloudStudio 增强层，不是 vendor parity。

## 六之一、当前判断（2026-09-02 中午）

- 路线清晰：四个根因都有单变量证据，阶梯上每一级的方向都能被电池和形态数字解释，没有"玄学"项。
- 距参考的剩余差距：P10 0.5 dB；长轴中位数 3 倍（13.5 vs 4.4 mm）；短轴 2.6 倍（1.14 vs 0.43）。前者靠更多 split 代数（满长 20 epoch 才有 8 个 reset 周期），后两者靠 R5/R6 的 opacity 排水让克隆不再堆叠、以及 flatten 比例目标的再标定。
- 已越过参考的：均值 PSNR、alpha 均值、alpha P05、长轴 p95。
- 不做的事：现在不开 20 epoch 长跑。理由有三：切片电池是训练视角分数，"均值 PSNR 超过参考"只说明拟合训练视角更好；R3 的 opacity 仍饱和（p50 0.56、>0.9 占 15%），split 子代继承饱和父本的 opacity（数值上已夹紧到 0.999999，但那是防崩溃不是画质），更多 split 代数可能制造更多近乎全不透明的子代而不是更锐；57% 遮挡穿透 LiDAR 目标与 33–63% 切片外监督仍在决定致密化的出生地。
- 顺序改为：C0 监督正确性（隐藏点剔除重建缓存、切片归属掩膜、DA2 远距有效性）→ 在可信目标上再做 loss 权重 → 拓扑（split/clone）→ opacity/cull → 形状正则。
- 长跑前必须有的证据：split 父本/子代 opacity 分布与血统（出生步、出生方式、300 步后的 opacity/尺寸）、以及最优短臂延长到 7k/10k 时 P10、alpha P05、长短轴、离群体都继续向参考收敛。

## 七、需要人决定的事项

- 是否接受 20 epoch 全长（三切片约 15 小时）作为交付训练预算。
- 是否允许重建数据集（隐藏点剔除 + 空间分块验证集），这会改变训练集签名、需要重跑 gate 链。
