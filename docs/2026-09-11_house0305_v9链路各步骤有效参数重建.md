# house0305 v9 预处理链：各步骤工具、argparse 与实际生效参数重建（2026-09-11）

来源：只读子任务对 `C:\Peter\3dgs-runs\house0305_sop\gates_v9\*.json|*.log`、`C:\Peter\3dgs-datasets\house0305_sop_v{8,9}\*` 各产物 manifest、`docs/HOUSE0305_SOP_RESUME_STATE.zh-CN.md`、`run_configs/house0305_tiles/drive_tiles.cmd`、`run_configs/house0305_tiles/v9/stage_v9_tiles.py` 与各工具 argparse 的交叉核对。**没有任何工具记录自己的 argv**；下面的"实际生效值"全部从产物 manifest 里的 `projection_config / config / source_identity / input / sampling / model` 等字段反推，等价于命令行。

运行环境（`drive_tiles.cmd:6-9`）：`call C:\Peter\cloudstudio-3dgs-gate1\train\env_machine_b.cmd`，cwd `C:\Peter\cloudstudio-3dgs-gate1`，`RUN=C:\Peter\3dgs-runs\house0305_sop`，`PY=C:\Peter\cloudstudio-3dgs\.venv-train\Scripts\python.exe`。日志里的 traceback 显示 v9 实际是从 `C:\Peter\cloudstudio-3dgs-work` 的 tools 运行的。

## A1 `tools/audit_camera_time_sync.py`（时间同步审计，GPU）
产物 `RUN\time_sync_final`（是文件，84 KB）。生效值：`--config RUN\timesync_d1_config.json`（一个训练配置，`run_id house0305-d1-delivery-30k`）、`--checkpoint C:\Peter\3dgs-runs\probes\d1_30k\checkpoints\latest.pt`、`--base-dataset-manifest C:\Peter\3dgs-datasets\house0305_manifest_ba\dataset_manifest.json`、`--factor 1`、`--maximum-rig-frames 80`、`--offset-ms -10 -5 0 5 10 20`、`--reanalyze RUN\time_sync_f1`；结论 `best_offset_ms = 0.0`（"10 ms 只赚 0.008 dB，p=0.16，不过 alpha=0.05"）。它渲染一个**已训练**的模型，因此需要 CUDA 且需要先有一个模型。argparse：`--config --checkpoint --base-dataset-manifest --offset-ms(+) --factor{1,2,4}=4 --maximum-rig-frames=8 --output --significance-alpha=0.05 --reanalyze`。
`house0305_timefix/timefix_poses.json` 是另一条实验产物（`experiments/diagnostics/solve_time_shift_poses.py`），v9 未引用。

## A2 `tools/run_hloc_aliked_lightglue.py`（CUDA）
产物 `RUN\features_masked\feature_runtime_manifest.json`。argparse：`--image-dir --pairs --output --require-cuda --base-features --base-feature-runtime-manifest --dataset-manifest --mask-manifest --mask-root --person-mask-manifest --person-mask-root --overwrite|--resume --runtime-lock=upstream/rig_ba.lock.json --allow-unverified-vcs`。pairs 文件 `RUN\pairs.txt` 由 `tools/build_independent_at_pairs.py` 生成（默认 `--temporal-neighbors 4 --loop-max-distance-m 1.5 --loop-min-frame-gap 30 --loop-neighbors 4`）。

## A3 `tools/run_hloc_triangulation.py`（CPU / pycolmap）
产物 `RUN\triangulation\triangulation_runtime_manifest.json`。argparse：`--image-dir`、`--reference-model | --dataset-manifest`、`--pairs --features --matches --feature-runtime-manifest --output`（必填）、`--runtime-lock --allow-unverified-vcs`。

## A4 `tools/run_independent_at.py`（采用路径）/ `tools/run_rig_ba.py`（CPU）
v8 manifest 的 `training_lineage`：`camera_policy = accepted_independent_at_shared_calibration`，`independent_at_algorithm_version = independent_pos_prior_shared_single_focal_kb4_at_v2`，`pose_policy = accepted_independent_at_all_images`；产物 `RUN\at_v7c\at_report.json` 与 `at_v7c\candidate_model`。同时 `pose_correction.algorithm_version = rig_ba_stage1_prior0.01m_candidate_v1`（`run_rig_ba.py --position-prior-stddev-m 0.01 --through-stage stage_1`，替换 796 张位姿）。
`run_independent_at.py` 默认：`--position-sigma-x/y/z 0.03/0.03/0.06 --pose-iterations 150 --full-iterations 250 --intrinsic-mode single_focal_kb4 --intrinsic-outer-iterations 8 --intrinsic-max-nfev 50 --outer-pose-iterations 50 …`，可选 `--mipmap-summary --triangulation-runtime-manifest`。

## A5 `tools/build_ba_training_manifest.py`（CPU）
产物 `house0305_sop_v8\dataset_manifest.json`（sha `33f5f814…`，下游全部绑定它）。argparse：`--manifest --split-manifest (--ba-report | --independent-at-report) --candidate-model --output --force`。

## B 遮罩与划分
v9 **复用 v8** 的圆形遮罩、人物遮罩与 dataset manifest，只新建了 `house0305_sop_v9\split_manifest.json`。
- 圆形遮罩：`tools/build_per_image_masks.py` → `cloudstudio_3dgs/data/mask_manifest.py`：`--manifest --output --theta-max-deg=95.0 --valid-radius-px --force`；实际 `theta_max_deg 95.0`。
- 人物遮罩（GPU，Mask R-CNN）：`tools/build_person_masks.py --manifest --base-mask-manifest --recording-root --weights --output --device cuda:0 --inference-max-dimension 800 --score-threshold 0.65 --mask-threshold 0.5 --dilation-pixels 12 --review-frames-per-camera 25`（全部默认）；模型 `maskrcnn_resnet50_fpn_v2 / COCO_V1`，886 张 / 1216 实例；重绑定用 `tools/rebind_person_mask_base.py`。
- 划分：`tools/build_split_manifest.py --mode manual --manual house0305_sop_v9\manual_split.json --golden-rig-frames 1`（884/2，443 rig 帧）；其它模式 `temporal_block | spatial_block`，默认 `--validation-fraction 0.1 --temporal-block-count 10 --golden-rig-frames 8`。

## C `tools/build_face_cache.py --face-plan mipmap_face4`（CPU 多进程）
必填 `--dataset-manifest --recording-root --mask-manifest --mask-root --split-manifest --output`；可选 `--person-mask-manifest --person-mask-root --depth-manifest --depth-root --fov-deg=190 --face-plan{adaptive_full_fov(默认!),mipmap_face4} --workers --split{train,val} --no-verify --no-skip-existing`。生效：`face_plan mipmap_face4, fov 190, min_face_weight 0.02`（模块常量 `MIN_FACE_WEIGHT`，非 CLI）；884 张 → 3536 面（yaw_minus_35 / yaw_plus_35 / pitch_up_56 / pitch_down_56，尺寸 [1456, 2912]）。

## D `tools/build_renderer_mask_manifest.py`（CPU，秒级）
`--face-manifest --face-cache-root --output --no-verify-artifacts`；策略 `mipmap_renderer_visibility_compat_v1`，keep 表达式 `face_cache_combined_mask != 0`。

## E `tools/build_depth_cache.py`（CPU）
v9 复用 v8 `depth`。`--manifest --mask-manifest --point-cloud --output --workers=1 --max-images --max-points --min-range-m=0.2 --max-range-m=80 --max-theta-deg=95 --compact-provenance --force`。生效：`kb4_ray_zbuffer_v1`，`--max-points 8000000`（保留 6,252,623 点），`--compact-provenance`，886 张。

## F `tools/build_face4_lidar_geometry.py`（CPU 单进程）
必填 `--face-manifest --face-root --dataset-manifest --depth-manifest --depth-root --output`；`--point-cloud --storage-profile{audit,trainer_compact}=audit --workers=1 --visibility-cell-px=0 --visibility-tolerance=0.2 --visibility-margin-m=0.1`。v9 vis6 生效：`visibility_cell_px 6`，其余默认，`storage_profile audit`；3536 面 / 959,390,055 有效像素。

## G `tools/build_da2_face_cache.py`（GPU）
文档记录的命令（`HOUSE0305_SOP_RESUME_STATE.zh-CN.md:23-27`）：`--face-manifest … --face-root … --dataset-manifest … --depth-manifest … --depth-root … --model-source %SRC% --checkpoint %CKPT% --output …\da2_train --device cuda`；另有 `--input-size=518 --max-faces --shard`。v9 用 5 分片再汇总；模型 `Depth Anything V2 Small (vits)`，对齐 `lidar_range = scale*da2 + shift`，RANSAC 2000 次，3536 面中 1903 面对齐有效。

## H 门禁链（CPU，秒级）
- `build_mipmap_frontend_gate.py`：15 个 Path 参数（`--raw-dataset --time-sync-report --raw-circle-mask --raw-person-mask --feature-runtime --triangulation-runtime --at-report --candidate-model --training-dataset --training-circle-mask --training-person-mask --split-manifest --face4-train --face4-val --output`），全部可从 `gate_10_frontend.json → evidence` 原样读出。
- `advance_mipmap_renderer_mask_gate.py --frontend-gate --train-renderer-mask --val-renderer-mask --output` → gate_11。
- `advance_mipmap_lidar_depth_gate.py --renderer-gate --depth-manifest --output` → gate_12。
- `advance_mipmap_da2_gate.py --lidar-gate --train-da2 --val-da2 --output` → gate_13。
- `advance_mipmap_sky_gate.py` **v9 未跑**（无 gate_14）；`advance_mipmap_tile_gate.py --lidar-depth-gate gate_12 --tile-plan … --deferral-reason "surface route: per-view prerendered backdrops stand in for the independent sky model, so that stage stays deferred" --output gate_15`（`route surface_only`，`deferred_stages [da2_monocular_depth, independent_sky_background]`）。

## I `tools/build_lidar_face4_adaptive_tile_plan.py`（CPU，torch 只用于查显存）
必填 8 个输入；`--lidar-depth-gate gate_12`；`--budget-gib 8.0`（显式）；默认 `--samples-per-raw-view 5000 --scene-padding-fraction 0.2 --gpu-reserve-gib 3 --budget-ceiling-gib 64 --target-gaussians 22450000`。生效：点 4,430,000 / 观测 3,367,938 / 3544 张；`residency_model {base 0.57 GiB, 0.5033 GiB/百万, lifecycle ×1.4}`；`leaf_count 4`（**输出**）。

## J–L 切片物化 / 几何
- `materialize_lidar_tile_inputs.py --tile-plan … --source-las "C:\baidunetdiskdownload\house\2026-03-05_10-58-54 - house\colorized.las" --expected-las-sha256 8bed917b… --output RUN\tile_inputs_v9`（无 `--voxel-decimate-m`）：4 切片，19,423,498 含 halo 点；Tile_0 7,044,777 点 / 2132 视角 / 42640 步，Tile_1 3,417,320 / 1829 / 36580，Tile_2 3,309,574 / 1684 / 33680，Tile_3 5,651,827 / 2317 / 46340。
- `build_mipmap_tile_geometry.py --tile-inputs --tile-inputs-root --output --batch-size=20000 --workers=-1` → `mipmap_k7_k30_tile_initialization_geometry_v1`。
- `build_tile_face4_lidar_geometry.py --tile-inputs --tile-inputs-root --tile-id N --face-manifest --dataset-manifest --source-geometry-manifest(vis6) --source-geometry-root --output-root`，逐切片串行。

## M 归属合同与训练门禁
- `build_tile_core_ownership_contract.py --tile-inputs --tile-inputs-root --output gates_v9\core_ownership.json`：`training_context core_plus_halo`，`export_scope core_owner_only`，`shared_boundary_rule minimum_tile_id`。
- `promote_surface_frozen_training_gate.py --upstream-gate gate_15 --ownership-contract … --fullres-smoke-manifest RUN\tile0_smoke_v9\run_manifest.json --tile-plan … --output gate_16`；DA2 解除延期后重跑得 gate_17（训练配置引用的就是它），`authorized_scope.capacity_cap 14000000`。

## N 天空与背景（GPU）
- `build_sky_dome.py --dataset-manifest --recording-root --depth-manifest --depth-root --output [--person-mask-manifest] --count=100000 --radius-m=250 --elevation-min-deg=-15 --elevation-max-deg=88 --frame-stride=4 --theta-max-deg=80 --min-samples=3 --seed=42`（默认）→ `C:\Peter\3dgs-runs\probes\sky_house0305.pt`（sha `a70c3f76…`）。
- `build_view_backgrounds.py --config <训练配置> --dome … --split train --output RUN\view_backgrounds_v9 --downsample 4 --background 1 1 1`（3536 视角）。
- `build_tile_view_backgrounds.py --background-manifest … --background-root … --tile-inputs … --face-manifest … --dataset-manifest … --tile-id N --output-root RUN\tile_backgrounds_v9\Tile_N`：tile0 2132 张（1418 裁剪 / 714 整帧）。

## O 配置与训练
- `run_configs/house0305_tiles/v9/stage_v9_tiles.py`：无 CLI，常量 `RUN/V8/V9`、基配置 `tile0_F5_keep_split05_t2h_20k.json`、`max_steps = 20×views`、`controlled_stop 20000`、`refine_stop 14000`、`prune_switch = steps//2`、`cap_max = int(points×1.34)`、`"Tile_0"→f"Tile_{tid}"` 字符串替换、`run_id = house0305-t{tid}-…`。
- `tools/train_gsplat.py --config <json>`（唯一参数）。

## 训练配置里的 house0305 字面量（v9 四切片）
`max_steps 42640/36580/33680/46340`，`cap_max 9440001/4579208/4434829/7573448`，`prune_switch_step 21320/18290/16840/23170`，`controlled_stop 20000`；共同常量：`background_color [1,1,1]`、`sh_degree 0`、`lidar_range_weight 0.05 robust_log_huber`、`da2_depth_weight 0.15`、`mono_depth_max_range_m 30`、`lidar_alpha_weight 0.5 / target 0.95 / dilation 6`、`lidar_normal_alignment {align 0.05, flatten 0.05, tangent_isotropy 0.1, planarity_gate 0.6, flatten_ratio_target 0.1}`、`geometry_regularization {anisotropy 0.01, max_world_size_m 0.2, world_shrink 0.8, max_anisotropy 80}`、`metric_scale_calibration {precomputed, knn 7}`、`grow_grad2d 7.5e-5`（后续臂 5e-5）、`split/prune_scale_m 0.2`、`detail_split_scale_m 0.005 / screen_radius 0.0035`、`learning_rates {means 1.6e-5, scales 0.005, quats 1e-3, opacities 0.05, colors 2.5e-3}`、`seed 42`。写死绝对路径：`recording_root C:\Peter\testdata\S1\house0305`、`person_mask_root`、`gsplat_lock`（gate1 检出）、`mipmap_pipeline_gate`。

## 各尺度值由谁推导
| 值 | 推导 |
| --- | --- |
| `grow_grad2d` | 无独立工具：由 `default_strategy_adapter._last_growth_event` 的 `gradient_quantiles / threshold_sweep_counts / footprint_weighted_audit` 在一次探针训练（`RUN\gradmetric_probe.json`）中读出；文档明写不可照抄 |
| `scene_scale` | 训练器内 `trainer.py:3586` 取初始化点到中位点距离的 P95；`split_scale_m/prune_scale_m` 除以它 |
| `reference_scale_m`（means lr / noise lr） | `tools/audit_metric_scale_calibration.py --ply --mode knn --knn-neighbors …`；v9 配置 `precomputed, knn 7` |
| `cap_max` / `max_steps` | stager：点数×1.34 / 20×视角数 |
| `--budget-gib` / 叶数 | 工具自身按 `min(gpu0_available, ceiling 64, system_available)` 解析；house0305 显式 8.0；叶数是输出 |
| `split/prune_scale_m 0.2`、`max_world_size_m 0.2`、`detail_split_*` | 无工具，竞品恢复常量 / 文档约定 |

## 对工程化的四点提醒
1. v9 没有 gate_14（天空）：编排器要建模 `surface_only` 分支（天空穹顶 + 视角背景 + 切片背景 + `--deferral-reason`）。
2. v9 是部分重建：dataset manifest、遮罩、人物遮罩、深度复用 v8；house0614 没有可复用的这一层。
3. `build_face_cache.py --face-plan` 默认值错（`adaptive_full_fov`）。
4. 工具在 `-work` 检出、`gsplat.lock` 在 `-gate1` 检出、venv 在 `cloudstudio-3dgs\.venv-train`：五处路径只由 `run_configs/pipeline.house0305.example.json` 钉住。
