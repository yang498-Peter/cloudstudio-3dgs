# snow 点云、照片、轨迹时间戳同步审计

审计日期：2026-08-27
数据目录：`G:\S1\USA\2026-02-24_16-21-11snow\process\2026-02-24_16-21-11snow_2`

## 结论

- **内部相对时间轴：通过。** LAS 点时间、双目照片文件名时间和 `odom.csv` 轨迹都使用同一组约 `17719717xx` 的传感器/GPS时间戳，没有发现秒级固定偏移、回退、重复或中途跳变。
- **绝对设备时间：异常。** 原始 MCAP 的设备/记录时间是 2025-12-15，而 GPS 时间是 2026-02-24；差值为 `6,152,711.6 s`（约 `71.21 d`）。`/rtk/time` 的 88 个样本均未证明 GPS/RTC 有效，但 LiDAR 报告 `timeSync=true`。
- **照片相对轨迹不需要额外平移。** 36 帧内容级渲染偏移复核中，`0 ms` 的平均 PSNR/SSIM 最佳；没有证据支持给照片或轨迹额外增加固定时间偏移。
- **启动段边界：** 原始左右相机各 176 张，`ImgPose.txt` 和 `.mpl` 各保留 171 张；每侧最早 5 张在轨迹/点云有效段之前，因此没有姿态。这是起始约 2.15 秒的覆盖边界，不是中途丢帧或逐渐漂移。

## 数值证据

### 点云 LAS

- 文件：`2026-02-24_16-21-11snow_colorized.las`
- 点格式：LAS 1.4 / point format 7，点数 `7,036,347`
- GPS time：全部有限、非零；范围 `1771971715.5415368` 到 `1771971801.5404040`，持续 `85.998867 s`
- 相对轨迹：点云起点晚于轨迹起点 `0.416 ms`，点云终点晚于最后轨迹样本 `99.833 ms`
- 连续性：有效段内部 859 个 100 ms 时间桶均有点，没有内部时间空洞

### 轨迹

- 文件：`odom.csv`
- 样本数 `860`，持续 `85.899450 s`
- 时间戳严格递增；间隔中位数 `99.840 ms`，最大 `103.576 ms`，无大间断

### 照片与姿态

- 原始照片：左 176、右 176；每侧时间戳严格递增，间隔中位数约 `500.031 ms`，无大间断
- 双目同帧绝对时间差：中位数 `0.004096 ms`，P95 `0.016960 ms`，最大 `0.068864 ms`
- 有姿态照片：左 171、右 171，全部位于轨迹覆盖范围内
- 有姿态照片到最近 10 Hz 轨迹样本的绝对距离：中位数约 `46.3 ms`，最大 `49.154 ms`。照片位于相邻轨迹帧之间，正确使用方式是插值，不是要求时间戳逐行相等。
- `.mpl` 中浮点秒时间戳回写纳秒文件名的误差为 0；`ImgPose.txt` 的十进制显示量化最大约 `119 ns`，可忽略。

### 内容级偏移复核

- 既有 36 帧 refined sweep：`0 ms` 为最佳，PSNR `18.464260 dB`，SSIM `0.558161`
- `+20 ms`：PSNR `18.462786 dB`，SSIM `0.556254`，没有形成可证明改善
- 粗扫中的负偏移与更大正偏移整体更差，因此不建议改时间戳或人为补偿

## 风险边界与建议

- 处理后的 LAS、照片、`ImgPose.txt`、`.mpl` 与 `odom.csv` 已经共用传感器/GPS时间轴；**不要只对其中一路补 71.21 天**，否则会人为破坏现有同步。
- 设备/MCAP记录时间与传感器/GPS时间不一致是真实 bug。若要把该数据与外部事件日志、系统时间或其他设备合并，必须先明确采用哪一种时基，并显式记录转换。
- 本审计证明时间戳结构和现有内容级偏移扫描没有明显错位；它不是高速动态场景下的硬件触发标定。若需要亚毫秒级正式验收，仍需同步灯/硬件触发或厂家原始时钟诊断。

## 证据文件

- `G:\cloudstudio-3dgs\results\diagnostics\snow-20260224-time-sync-scan-health\scan-health-facts.json`
- `G:\cloudstudio-3dgs\outputs\snow-20260224-full-20260825\time_sync_audit_v21a\render_sweep_refined.json`
- `G:\S1\USA\2026-02-24_16-21-11snow\process\2026-02-24_16-21-11snow_2\odom.csv`
- `G:\S1\USA\2026-02-24_16-21-11snow\process\2026-02-24_16-21-11snow_2\ImgPose.txt`

客户原始数据和现有处理结果均保持只读、未修改。
