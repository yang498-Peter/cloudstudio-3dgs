diff --git a/examples/datasets/colmap.py b/examples/datasets/colmap.py
index 2e9db27..f3630a9 100644
--- a/examples/datasets/colmap.py
+++ b/examples/datasets/colmap.py
@@ -129,2 +129,6 @@ class Parser:
         load_exposure: bool = False,
+        # S1 patch (2026-07-02): keep raw fisheye images for 3DGUT (--with_ut);
+        # loads circular valid-region masks from <data_dir>/masks/ instead of
+        # undistorting to pinhole.
+        keep_distortion: bool = False,
     ):
@@ -135,2 +139,3 @@ class Parser:
         self.load_exposure = load_exposure
+        self.keep_distortion = keep_distortion
 
@@ -225,5 +230,13 @@ class Parser:
         image_dir = os.path.join(data_dir, "images" + image_dir_suffix)
-        for d in [image_dir, colmap_image_dir]:
-            if not os.path.exists(d):
-                raise ValueError(f"Image folder {d} does not exist.")
+        if not os.path.exists(colmap_image_dir):
+            raise ValueError(f"Image folder {colmap_image_dir} does not exist.")
+        if not os.path.exists(image_dir):
+            if factor <= 1:
+                raise ValueError(f"Image folder {image_dir} does not exist.")
+            # S1 patch (2026-08-20): raw recordings do not ship pre-scaled
+            # images_<factor> folders. Materialize the requested PNG pyramid
+            # directly from images/ so first-run training is self-contained.
+            image_dir = _resize_image_folder(
+                colmap_image_dir, image_dir + "_png", factor=factor
+            )
 
@@ -374,2 +387,18 @@ class Parser:
             camtype = camtype_dict[camera_id]
+            if self.keep_distortion and camtype == "fisheye":
+                # S1 patch: skip undistortion — 3DGUT rasterizes the distorted
+                # camera directly. Load the circular valid-region mask instead
+                # (generated by tools/make_fisheye_masks.py, 255 = valid).
+                width, height = self.imsize_dict[camera_id]
+                first_name = image_names[camera_ids.index(camera_id)]
+                mask_path = os.path.join(
+                    data_dir, "masks", os.path.splitext(first_name)[0] + ".png"
+                )
+                if os.path.exists(mask_path):
+                    m = imageio.imread(mask_path)
+                    m = cv2.resize(m, (width, height), interpolation=cv2.INTER_NEAREST)
+                    self.mask_dict[camera_id] = m > 127
+                else:
+                    print(f"[Parser] keep_distortion: no mask found at {mask_path}")
+                continue
             assert camera_id in self.Ks_dict, f"Missing K for camera {camera_id}"
@@ -474,3 +503,3 @@ class Dataset:
 
-        if len(params) > 0:
+        if len(params) > 0 and not getattr(self.parser, "keep_distortion", False):
             # Images are distorted. Undistort them.
diff --git a/examples/simple_trainer.py b/examples/simple_trainer.py
index 7da3f9f..0495708 100644
--- a/examples/simple_trainer.py
+++ b/examples/simple_trainer.py
@@ -446,2 +446,4 @@ class Runner:
                 load_exposure=cfg.load_exposure,
+                # S1 patch (2026-07-02): opt-in raw-fisheye route for 3DGUT
+                keep_distortion=os.environ.get("S1_KEEP_FISHEYE", "") == "1",
             )
@@ -454,2 +456,25 @@ class Runner:
             self.valset = Dataset(self.parser, split="val")
+            if getattr(self.parser, "keep_distortion", False):
+                # S1 patch: feed fisheye distortion into the UT rasterization
+                # path through the same duck-typed per-camera table the ncore
+                # loader uses (rasterize_splats reads self.ncore_camera_data).
+                from types import SimpleNamespace
+
+                self.ncore_camera_data = {
+                    idx: SimpleNamespace(
+                        camera_model="fisheye",
+                        ftheta_coeffs=None,
+                        radial_coeffs=np.asarray(
+                            self.parser.params_dict[cid][:4], dtype=np.float32
+                        ),
+                        tangential_coeffs=None,
+                        thin_prism_coeffs=None,
+                    )
+                    for cid, idx in self.parser.camera_id_to_idx.items()
+                    if len(self.parser.params_dict[cid]) > 0
+                }
+                print(
+                    f"[S1] keep_distortion active: {len(self.ncore_camera_data)} "
+                    "fisheye cameras routed to UT rasterization"
+                )
         self.scene_scale = self.parser.scene_scale * 1.1 * cfg.global_scale
diff --git a/gsplat/cuda/build.py b/gsplat/cuda/build.py
index 1a3cbbf..d37e0f8 100644
--- a/gsplat/cuda/build.py
+++ b/gsplat/cuda/build.py
@@ -221,2 +221,10 @@ def get_build_parameters():
 
+    if sys.platform == "win32":
+        # ParallelBatch forward is not used by the default MixedBatch trainer
+        # and does not link reliably with the Windows CUDA 12.8 toolchain.
+        sources = [
+            s for s in sources
+            if not s.endswith("csrc/RasterizeToPixelsFromWorld3DGSParallelBatchFwd.cu")
+        ]
+
     extra_ldflags += [] if WITH_SYMBOLS or sys.platform == "win32" else ["-s"]
diff --git a/gsplat/cuda/csrc/Rasterization.cpp b/gsplat/cuda/csrc/Rasterization.cpp
index 204e218..ee87270 100644
--- a/gsplat/cuda/csrc/Rasterization.cpp
+++ b/gsplat/cuda/csrc/Rasterization.cpp
@@ -961,2 +961,9 @@ RasterizeToPixelsFromWorld3DGSFwdResult rasterize_to_pixels_from_world_3dgs_fwd(
     } else {
+#if defined(_WIN32)
+        TORCH_CHECK(
+            false,
+            "ParallelBatch 3DGUT rendering is not available in this Windows build; "
+            "use the default MixedBatch renderer"
+        );
+#else
         // The batch-parallel forward needs a transient per-batch summary
@@ -1027,2 +1034,3 @@ RasterizeToPixelsFromWorld3DGSFwdResult rasterize_to_pixels_from_world_3dgs_fwd(
         );
+#endif
     }
diff --git a/gsplat/cuda/csrc/RasterizeToPixelsFromWorld3DGSParallelBatchBwd.cu b/gsplat/cuda/csrc/RasterizeToPixelsFromWorld3DGSParallelBatchBwd.cu
index abbbe6e..1dc30d6 100644
--- a/gsplat/cuda/csrc/RasterizeToPixelsFromWorld3DGSParallelBatchBwd.cu
+++ b/gsplat/cuda/csrc/RasterizeToPixelsFromWorld3DGSParallelBatchBwd.cu
@@ -156,2 +156,3 @@ std::tuple<at::Tensor, at::Tensor, int64_t> compute_batch_csr(
 
+#if !defined(_WIN32)
 static __global__ void emit_round_major_pairs_kernel(
@@ -188,3 +189,11 @@ uint32_t ceil_log2_u64(uint64_t x) {
     }
+#if defined(_MSC_VER)
+    // S1 patch (2026-07-02): __builtin_clzll is a GCC/Clang builtin; MSVC
+    // host compilation needs _BitScanReverse64 instead.
+    unsigned long idx;
+    _BitScanReverse64(&idx, x - 1);
+    return static_cast<uint32_t>(idx) + 1u;
+#else
     return 64u - static_cast<uint32_t>(__builtin_clzll(x - 1));
+#endif
 }
@@ -250,2 +259,3 @@ at::Tensor compute_bid_to_slot(
 }
+#endif
 
